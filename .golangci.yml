version: "2"

linters:
  settings:
    errcheck:
      # CLI writes to its own stdout/stderr; failures there are not actionable.
      exclude-functions:
        - fmt.Fprint
        - fmt.Fprintf
        - fmt.Fprintln
