# Claim-verification evidence (issue #81)

Machine-generated appendix to `2026-08-05-merged-v1-security-review.md`.
Each entry is one SECURITY.md or spec claim an auditor confirmed against the
code, with the file:line evidence it cited. Produced by an eight-cluster
adversarial audit of `origin/main` at `ec615b9`; every candidate finding was
put to an independent refuter before reaching the report.

Claims verified: 100. Candidate findings raised: 34. Refuted: 27. Surviving: 7.

## Reading notes

Two clarifications on the generated entries below, added after review. The
entries themselves are left as recorded.

- **`decodeFleet` validates non-empty fields.** Entries describing
  `@agentctl_fleet` as "re-validated field by field" are accurate for the role
  name and harness, which are always checked, but the model and effort checks
  are guarded by `if fields[2] != ""` and `if fields[3] != ""`
  (`internal/fleet/relaunch.go:564-573`). An empty field is the encoding of a
  defaulted model or effort, so skipping it is correct — `EncodeFleet` writes
  the empty string for exactly that case — but "field by field" reads slightly
  stronger than the code. Nothing unvalidated reaches a harness argv either
  way: `Spec.ModelArgs` and `Spec.EffortArgs` render no arguments at all for an
  empty value (`internal/harness/harness.go:46-59`).
- **Line numbers pin to `ec615b9`.** Every `file:line` citation was taken
  against `origin/main` at `ec615b9`. Later commits move them; the surrounding
  quoted code is the durable identifier.

## The quoting layer end-to-end (shellq → harness.AgentArgv → fleet.agentCommand → tmuxx.NewSession/NewWindow)

- **"The only shell-interpreted string in the system (the window command tmux runs) is assembled by a dedicated ... quoting layer" — shellq.Join has exactly one non-test call site and it is the window command.**
  - internal/shellq/shellq.go:15 (Join) is referenced in non-test code only at internal/fleet/fleet.go:264 (`return "exec " + shellq.Join(argv), nil`), reached from internal/fleet/fleet.go:225 (newSession) and internal/fleet/fleet.go:236 (newWindow). Grep over all non-test .go files returns no other `shellq.` reference.
- **Quote is total and correct for every byte except NUL.**
  - internal/shellq/shellq.go:11 wraps the value in single quotes and rewrites each `'` as `'"'"'`. strings.ReplaceAll operates on bytes, so invalid UTF-8, \n, \\, $, `, *, ~, ${IFS}, ; and control bytes are all carried through verbatim inside a single-quoted word — no POSIX shell metacharacter has meaning inside single quotes. NUL is documented as out of contract at internal/shellq/shellq.go:3-4, and no token that reaches Join can contain NUL (see the validation claims below); had one, os/exec would reject the argv element with EINVAL at internal/tmuxx/runner.go:31 rather than mis-execute.
- **"fuzz-tested" — the fuzz target genuinely round-trips through a real shell, not just string shape.**
  - internal/shellq/shellq_test.go:95-116 (FuzzQuoteRoundTrip, 12 seeds) calls assertQuoteRoundTrip at internal/shellq/shellq_test.go:118-133, which runs `sh -c "set -- <Quote(input)>; printf '%s:' \"$#\"; printf %s \"$1\""` and asserts both the word count is exactly 1 and $1 equals the input byte for byte. I ran `go test ./internal/shellq -run xxx -fuzz FuzzQuoteRoundTrip -fuzztime 45s`: 48,837 executions, PASS, no crashers.
- **Every token that reaches shellq.Join on the LAUNCH path has passed validation.**
  - internal/harness/harness.go:68 builds argv = ["amq","coop","exec","--session",session,"--me",role,spec.Executable]. "amq"/"coop"/"exec"/"--session"/"--me"/"--" are Go string literals; spec.Executable is a literal from the closed registry at internal/harness/harness.go:21-34 ("claude"/"codex") selected via Lookup at internal/harness/harness.go:37-43, which errors for any other name (internal/harness/harness.go:63-66). session passed config.ValidateSessionName (cmd/agentctl/main.go:209 and again at main.go:520) against `^[a-z0-9][a-z0-9_-]*$` (internal/config/config.go:11,102). role.Name passed the same pattern in parseRoles (internal/config/config.go:223). Model reaches argv only via longModelArgs (internal/harness/harness.go:77-79) after modelExpression `^[a-zA-Z0-9][a-zA-Z0-9._-]*$` (internal/config/config.go:12,275). Effort reaches argv only via claudeEffortArgs/codexEffortArgs after the closed-set check supportsEffort (internal/config/config.go:327,341-348).
- **Every token that reaches shellq.Join on the RELAUNCH path is validated too, including values sourced from tmux metadata.**
  - session.Name comes from internal/session/resolver.go:132-147, which only ever returns a session whose Name is byte-equal to a name already passed through config.ValidateSessionName (resolver.go:101, 108 or 126) — so the `--session` token is validated even though it was read back from `tmux list-sessions`. Stored @agentctl_fleet is re-validated field by field in decodeFleet: role name at internal/fleet/relaunch.go:553, harness at :560, model at :564-567, effort at :569-572, with the comment at :542-543 stating exactly why. Flag overrides are validated twice, at cmd/agentctl/main.go:562-583 and again at internal/fleet/relaunch.go:211-232. The legacy path (internal/fleet/relaunch.go:406-427) uses request.Role (validated at relaunch.go:211) and ParseHarness at :410.
- **The `exec ` prefix at fleet.go:264 is safe.**
  - internal/fleet/fleet.go:264 concatenates the constant "exec " with the fully quoted word list. Everything after it is a single-quoted shell word, so `exec` can only ever receive the intended words; argv is never empty (internal/harness/harness.go:68 always yields 8 tokens or an error), so the string is never the bare `exec `. The first word is always the literal 'amq', so `exec` can never be handed an option-looking first argument.
- **The assembled string reaches tmux as ONE argv element after `--`, and flag injection through it is impossible.**
  - internal/tmuxx/tmux.go:130-132 (new-session) and :168-170 (new-window) append `"-P", "-F", format, "--", command` as the final elements of a []string handed to exec.CommandContext (internal/tmuxx/runner.go:31) — one element, after `--`. Values that precede `--` are either literals, validated names (`-s name`, `-n role`), or option values consumed positionally (`-c dir`, `-e NAME=VALUE`), so a leading dash cannot be read as a flag. I confirmed this end-to-end on a throwaway socket (`tmux -L agentctl-audit-shellq`, tmux 3.7b): a command built as `"exec " + shellq.Join(tokens)` with tokens including `s'; touch /tmp/PWNED; echo '`, `$(touch /tmp/PWNED2)`, `` `touch /tmp/PWNED3` ``, an embedded newline, a trailing backslash, `*`, `~`, `${IFS}` and the invalid-UTF-8 bytes \x01\x02\xff was delivered to the child process as argv identical byte for byte, and none of /tmp/PWNED{,2,3} was created. Server killed afterwards.
- **"agentctl itself never invokes a shell; all tmux calls are argv arrays."**
  - internal/tmuxx/runner.go:31 and :37 are the only production process launches and both use exec.CommandContext(ctx, executable, args...) with no shell. Grep for `sh -c`, `/bin/sh`, `bash -c` over non-test cmd/ and internal/ returns nothing; the only `sh -c` in the repo is the round-trip assertion in internal/shellq/shellq_test.go:125.
- **"no caller-supplied text reaches the codex `--config 'model_reasoning_effort=\"…\"'` expression, whose value portion codex parses as TOML" — %q and TOML agree for every value that can reach it.**
  - internal/harness/harness.go:94 renders fmt.Sprintf("model_reasoning_effort=%q", effort). The only values that can reach it are members of effortLevels[HarnessCodex] = {low, medium, high, xhigh, max} (internal/config/config.go:38), enforced at internal/config/config.go:327 on launch and internal/fleet/relaunch.go:569-572 on relaunch. I rendered all five: they produce model_reasoning_effort="low" … ="max" — plain ASCII letters, where Go's %q escaping and TOML basic-string escaping are identical. (The escaping rules do differ in general: Go emits \a, \v and \xNN, which TOML rejects; the divergence would surface as a codex parse error, not as injection, and no reachable value triggers it.)
- **Both name/model regexes are anchored to end-of-TEXT, so a trailing newline cannot slip through.**
  - internal/config/config.go:11-18 compile the patterns without the (?m) flag; Go's regexp `$` means end of text with no Perl-style trailing-newline exemption. Empirically confirmed: "abc\n", "abc\nrm -rf", "a\x00b" and "a b" all fail both nameExpression and modelExpression.
- **No other non-test site builds a string a shell or another parser re-interprets from caller-influenced values.**
  - The tmux -e environment pairs are separate argv elements built at internal/tmuxx/tmux.go:193-202 from the validated session/role and the literal "1" (internal/fleet/fleet.go:248-254); no shell string is formed from them. internal/tmuxx/control.go:23-38 passes the payload as one argv element after `-l --` (closed registry, argument-free). internal/tmuxx/control.go:52 runs `ps -o comm= -p <int>`. All other fmt.Sprintf hits in non-test code are Error() strings and status output, none of which is re-parsed or executed.
- **The stored-metadata relaunch path cannot panic on a missing role index before reaching the quoting layer.**
  - internal/fleet/relaunch.go:380 indexes storedRoles with roleIndex(), which returns -1 when absent. That is unreachable: relaunch.go:330 requires the requested role to be in the roster and relaunch.go:365 requires sameRoles(storedRoles, rosterRoles) — equal length and name-for-name equality (relaunch.go:579-589) — and both parseRoster (:534) and decodeFleet (:557) reject duplicates. The invariant holds, but it is carried by two separate checks rather than by construction.

## validation completeness (spec §7)

- **Go's `$` in these patterns is end-of-TEXT, not end-of-line, so "claude\n--evil" cannot pass ValidateModelName. The SECURITY.md claim that a value like `--dangerously-bypass-approvals-and-sandbox` cannot be passed through the model slot holds, including via a multiline bypass.**
  - internal/config/config.go:12 defines modelPattern `^[a-zA-Z0-9][a-zA-Z0-9._-]*$`; config.go:17 compiles it with regexp.MustCompile (Perl syntax, OneLine flag set, no (?m)). Empirically confirmed with a standalone Go program using the identical pattern: "claude\n--dangerously-bypass-approvals-and-sandbox" -> false, "claude\n" -> false, "claude\r" -> false, "claude\r\n" -> false, "claude\x00" -> false, "claude " -> false, "claude\tx" -> false, "claude'x" -> false, "claude$(id)" -> false, "--evil" -> false, "" -> false; "claude" and "claude-opus-4.5" -> true. Same result for namePattern (config.go:11): "sess\n--x" -> false, "sess\n" -> false, "-sess" -> false, "SESS" -> false.
- **Every production entry point that accepts a SESSION name validates it against ^[a-z0-9][a-z0-9_-]*$ before any tmux command runs. No entry point skips it.**
  - launch: cmd/agentctl/main.go:209 (config.ValidateSessionName before config.ParseFleet and before fleet.Launch). relaunch/attach/status --session/clear/compact/kill all reach the name only through deps.resolver.Resolve at cmd/agentctl/main.go:251, and internal/session/resolver.go:101 validates the explicit --session value as the first act of Resolve. AGENTCTL_SESSION is validated at internal/session/resolver.go:108. The ambient current-tmux-session name returned by display-message is validated at internal/session/resolver.go:126, so a hand-made tmux session named e.g. "My Session" produces a ResolutionError rather than being used. `status` without --session (cmd/agentctl/main.go:244) never takes a caller-supplied name at all; it enumerates via ListSessions.
- **Every production entry point that accepts a ROLE name validates it against the same charset, and the control path re-validates defensively at a second layer.**
  - cmd/agentctl/main.go:230 validates the positional ROLE for clear/compact/relaunch. internal/target/resolver.go:41 re-validates it as the first gate of the control chain. internal/fleet/relaunch.go:211 re-validates it as the first act of Relaunch. Launch-side role names are validated in internal/config/config.go:223 (--roles), :272 (--models entries), :316 (--efforts entries). Roles decoded from the stored @agentctl_fleet metadata are re-validated at internal/fleet/relaunch.go:553.
- **Model identifiers are charset-validated at every entry point, including the stored-metadata decode path.**
  - internal/config/config.go:275 (launch --models), cmd/agentctl/main.go:569 (relaunch --model, before any tmux call), internal/fleet/relaunch.go:220 (re-validated inside Relaunch), internal/fleet/relaunch.go:565 (re-validated when decoding @agentctl_fleet).
- **SECURITY.md's claim that "Effort levels are matched against an allowlist rather than a charset, so the effort slot cannot carry a flag, a quote, or a newline at all" is true.**
  - internal/config/config.go:341-348 supportsEffort does a literal string equality scan over internal/config/config.go:36-39, which contains only {low, medium, high, xhigh, max} for both harnesses. There is no charset fallback anywhere in the effort path. Consequently every value that reaches internal/harness/harness.go:94 codexEffortArgs (`--config model_reasoning_effort=%q`) is one of five safe tokens.
- **Duplicate command-line options are rejected, and a value or positional beginning with "-" fails closed rather than being smuggled past a validator.**
  - internal/cliflags/cliflags.go:82-89 onceValue.Set returns DuplicateOptionError on the second occurrence and stashes it in s.parseError, which Parse returns preferentially (cliflags.go:55-62); both parseLaunch (main.go:396) and parseCommand (main.go:480) use it. Verified against the built binary: `clear --session -foo bar` -> "invalid session \"-foo\": must match ^[a-z0-9][a-z0-9_-]*$"; `clear --session sess -role` -> "flag provided but not defined: -role"; `clear --session sess -- -role` -> "invalid role \"-role\""; `clear --session sess --session other role` -> "--session provided more than once". All exit 2 (usage) with no tmux call. Go's flag package also stops parsing at the first positional, so `clear ROLE --session x` yields 3 positionals and a usage error rather than a silently-ignored --session.
- **The --dir value never reaches the one shell-interpreted string; it is only ever a standalone argv element, and a dash-leading directory name is consumed correctly by tmux's getopt.**
  - internal/fleet/fleet.go:256-265 agentCommand builds argv from session, role.Name, harness, model and effort only -- the directory is not in it -- and internal/shellq/shellq.go quotes every token with single quotes. The directory reaches tmux only as a separate `-c` argv element (internal/tmuxx/tmux.go:130 and :168) and as a set-option value (internal/fleet/fleet.go:284 -> internal/tmuxx/tmux.go:213). internal/tmuxx/runner.go uses exec.CommandContext with argv arrays, so no shell is involved. Empirically on tmux 3.7b (throwaway socket agentctl-audit-40266, since killed): `new-window -c -weird` created a pane whose pane_current_path was the directory literally named "-weird"; the leading dash was consumed as -c's argument, not re-parsed as an option.
- **harness.AgentArgv performs no value validation of its own; it relies entirely on internal/config having validated first.**
  - internal/harness/harness.go:62-75 -- the only check is registry Lookup of the harness name; Model and Effort are passed straight through to longModelArgs (harness.go:77) and claudeEffortArgs/codexEffortArgs (harness.go:83, :93).
- **Stored @agentctl_fleet metadata is re-validated field-by-field on read, as residual 4 of SECURITY.md claims.**
  - internal/fleet/relaunch.go:544-577 decodeFleet: exactly-4-field structure (:549-551), role charset (:553), duplicate role (:556), harness allowlist (:560), model charset (:565), effort against the entry's own decoded harness (:570). The roster is separately cross-checked for equality with the decoded roles at relaunch.go:365.

## Target-validation chain and its TOCTOU window (internal/target, internal/session, internal/control, internal/tmuxx)

- **"ROLE charset validated before any command runs" — holds in both the CLI composition and the library composition.**
  - cmd/agentctl/main.go:229-233 calls config.ValidateRoleName for clear/compact/relaunch before deps.resolver.Resolve at cmd/agentctl/main.go:251, which is the first call that runs a tmux command. Independently, internal/target/resolver.go:41 validates the role before any r.client call. parseCommand (main.go:221) is pure flag parsing and issues no external command.
- **"session resolved by exact comparison and addressed by tmux ID".**
  - internal/session/resolver.go:139 compares candidate.Name == name with no normalisation or globbing; internal/session/resolver.go:143 requires exactly one match and otherwise returns *ResolutionError. Every downstream tmux call addresses session.ID, not the name: internal/target/resolver.go:44, :51, :58, and internal/tmuxx/tmux.go:237/261 reject anything that is not $<digits> via validateID (internal/tmuxx/tmux.go:427-437).
- **"session is agentctl-managed at the expected version" — two independent session-option gates, both fail closed on error and on empty.**
  - internal/target/resolver.go:44-50 requires @agentctl_managed == "1"; internal/target/resolver.go:51-57 requires @agentctl_version == "1". A tmux failure becomes tmuxx.ClassifyError (resolver.go:46, :53), which never returns nil for a non-nil input (internal/tmuxx/error.go:33-41). An unset option returns empty (internal/tmuxx/tmux.go:240-244 uses show-options -qv) and is refused as SessionMetadataError. Measured on tmux 3.7b, throwaway socket: `show-options -qv -t $0 @agentctl_managed` returns EMPTY even when `set -g @agentctl_managed 1` is in effect, so a stray global user option cannot make an unmanaged session controllable.
- **"window resolved by exact comparison and addressed by tmux ID, with more than one match refused".**
  - internal/target/resolver.go:65 matches window.Name == role exactly; internal/target/resolver.go:70 refuses len(matches) != 1, which covers both zero matches and two-or-more (the error at internal/target/errors.go:30-39 reports every colliding window ID). The selected window is thereafter addressed only by window.ID (internal/target/resolver.go:77 -> internal/tmuxx/tmux.go:299).
- **"stored role metadata matches" — and this is the gate that actually carries window ownership (see finding 1).**
  - internal/target/resolver.go:74 second clause, window.Role != role. Measured on tmux 3.7b: a hand-created window in a session stamped with @agentctl_managed=1 reports @agentctl_role as EMPTY, because agentctl stamps @agentctl_role only at window scope (internal/fleet/fleet.go:290 via SetWindowOption, internal/tmuxx/tmux.go:231) and never at session scope (internal/fleet/fleet.go:266-284).
- **"exactly one pane" — implemented as two independent counts, and the zero-pane case is caught.**
  - internal/target/resolver.go:81 refuses len(panes) != 1, which includes len==0 (internal/tmuxx/tmux.go:308-309 returns nil,nil for empty list-panes output). internal/target/resolver.go:85 additionally cross-checks the pane's own #{window_panes} field. cmd/agentctl/main.go:704-711 switches on len(paneState.Panes) != 1 BEFORE indexing Panes[0], so the zero-pane error path cannot panic.
- **"pane alive" is checked BEFORE the process-identity read, exactly as the document orders it.**
  - internal/target/resolver.go:88-90 returns on pane.Dead; the @agentctl_process baseline read is at internal/target/resolver.go:91 and the ps call at internal/target/resolver.go:94. pane_dead is parsed strictly, rejecting any value other than "0"/"1" (internal/tmuxx/tmux.go:324-332).
- **"the pane's root-process executable exactly equals the identity recorded at launch (@agentctl_process)" — exact string equality against a baseline taken from the same observation source.**
  - internal/target/resolver.go:101 is a plain != on two strings. The runtime value comes from `ps -o comm= -p <pane_pid>` (internal/tmuxx/control.go:48) with exactly one trailing newline trimmed (control.go:60) and empty output refused as ErrProcessUnavailable (control.go:62). The launch baseline is taken from the identical Client.ProcessName call (internal/fleet/fleet.go:309 via processBaseline) and stamped as @agentctl_process (internal/fleet/fleet.go:303). An empty baseline is refused outright at internal/target/resolver.go:91-93.
- **"the target pane is not the caller's own pane" — present, and immune to pane-ID reuse.**
  - internal/target/resolver.go:110-118 compares os.LookupEnv("TMUX_PANE") against string(pane.ID) and returns SelfTargetError on equality. Because tmux 3.7b never reuses pane IDs (verified empirically: %1 killed -> next allocation is %2), a merely stale TMUX_PANE value can never collide with a different live pane on the same server, so the guard has no false-negative arising from ID recycling.
- **"Delivery targets the resolved pane ID."**
  - internal/control/dispatcher.go:37-40 returns on any resolve error and passes only the returned paneID to DeliverPayload at internal/control/dispatcher.go:41. internal/tmuxx/control.go:20 re-validates the ID shape (%<digits>) before use, and all three send-keys invocations target string(paneID): control.go:23 (C-u), :26 (-l -- payload), :38 (Enter). No name, index, or format string reaches -t.
- **The chain runs identically for clear and compact, and no control path reaches DeliverPayload without going through Resolve.**
  - cmd/agentctl/main.go:285-291 is a single branch handling both commands through the same deps.controller.Execute. Both payloads are argument-free constants in the closed registry (internal/control/registry.go:15-22). DeliverPayload has exactly one non-test call site, internal/control/dispatcher.go:41, and Dispatcher.Execute unconditionally calls d.resolver.Resolve at internal/control/dispatcher.go:37 and returns on its error. main.go:139 wires control.New(targetResolver, client) as the only production Dispatcher.
- **No error path inside Resolve skips a later link; every failure is an immediate return.**
  - internal/target/resolver.go returns at 42, 46, 49, 53, 56, 60, 71, 75, 79, 82, 86, 89, 92, 97, 99, 102 and 111. There is no continue, no error accumulator, and no branch that falls through to pane.ID. The single success return is internal/target/resolver.go:119, reachable only after all twelve gates pass.
- **TOCTOU between the identity check and delivery cannot produce wrong-target delivery, because a vanished pane ID fails rather than aliasing.**
  - Measured on tmux 3.7b, throwaway socket: `send-keys -t %GONE` exits 1 ("can't find pane") for C-u, -l and Enter alike, and pane IDs are never reused. internal/tmuxx/control.go:23/26/38 propagate that failure, internal/control/dispatcher.go:42 classifies it, and cmd/agentctl/main.go:731-735 maps it to exitTmux. Separately measured: respawn-pane -k during the 1s payloadDelay (internal/tmuxx/control.go:12,30-36) discards the typed payload with the old pty — the new process received only a bare Enter (`GOT:[]`), not the payload.

## Payload registry, and rollback/kill ownership

- **"The control surface is a hardcoded registry of predefined commands (clear → /clear, compact → /compact in v1). Every payload is a complete constant."**
  - internal/control/registry.go:14-22 — payloads are untyped string constants `clearPayload = "/clear"` and `compactPayload = "/compact"`, held in a fixed-size array `var commands = [...]Command{...}` (registry.go:19). Grep over the whole tree shows `commands` is only ever read (registry.go:35,36,42); there is no assignment, append, init() or registration function anywhere, in test or non-test code.
- **Operations() and Lookup() cannot be used to introduce a payload.**
  - internal/control/registry.go:34-38 — Operations() allocates a new slice and `copy`s the array's values into it, so a caller mutating the result cannot reach `commands` (Command has only string fields, so there is no shared backing storage). internal/control/registry.go:41-48 — Lookup() is a linear exact-string comparison over the same array and returns `Command{}` plus *UnknownOperationError for anything else; it never constructs a Command from its argument. Operations() has no non-test caller at all (grep: only registry_test.go:16,19).
- **"There is no option, environment variable, or stdin path by which caller-supplied text can reach tmux send-keys."**
  - The only non-test caller of DeliverPayload is internal/control/dispatcher.go:41, and its payload argument is `command.Payload` taken straight from Lookup (dispatcher.go:33). The only caller of Dispatcher.Execute is cmd/agentctl/main.go:286, reached only inside `if command == "clear" || command == "compact"` (main.go:285), where `command` is `arguments[0]` (main.go:194) already required to be a key of `commandUsage` (main.go:195-198). The role/session arguments go to the resolver (dispatcher.go:37), which the TargetResolver interface gives no delivery capability (dispatcher.go:11-13). No environment variable or stdin is read anywhere on this path.
- **No format string or concatenation exists between Lookup and send-keys.**
  - internal/control/dispatcher.go:41 passes command.Payload by value; internal/tmuxx/control.go:26 passes `payload` as its own argv element in a variadic call; internal/tmuxx/tmux.go:394-400 forwards `args...` unchanged to the Runner; internal/tmuxx/runner.go:30-32 calls `exec.CommandContext(ctx, executable, args...)`. The only fmt.Sprintf on this path (control.go:21) formats an error message, not an argv element.
- **"Payloads are sent in tmux literal mode with Enter as a separate event." — and `-l --` is correct and sufficient.**
  - internal/tmuxx/control.go:23,26,38 issue exactly three send-keys calls: `send-keys -t %ID C-u`, `send-keys -t %ID -l -- PAYLOAD`, and (after the timer at control.go:30-36) `send-keys -t %ID Enter`. Asserted element-by-element at internal/tmuxx/control_test.go:18-20 and end-to-end at cmd/agentctl/main_control_test.go:342-344. Measured on tmux 3.7b (throwaway socket, server killed afterwards): `send-keys -l -- "-x --dangerous /clear"` delivered the bytes `-x --dangerous /clear` verbatim into the pane (od -c evidence), while the same call without `--` for the argument `-N5` consumed it as a tmux option and typed nothing. `--` is therefore load-bearing, and it is present.
- **C-u (the input clear) is a fixed constant, not caller-derived.**
  - internal/tmuxx/control.go:23 — the literal string "C-u" is written inline in the argv. It is not read from harness.Spec, from configuration, or from any argument. Because that call omits -l, tmux interprets it as a key; the payload call includes -l, which I confirmed on 3.7b types the characters "C-u" rather than pressing the key.
- **Delivery is refused before any tmux call unless the target is a typed pane ID.**
  - internal/tmuxx/control.go:20-22 — validateID(paneID, '%') runs before the first tmuxOutput; validateID (tmux.go:427-437) requires the prefix byte plus at least one character, all digits. internal/tmuxx/control_test.go:72-82 asserts zero runner calls for a name target.
- **The ownership instant: agentctl owns the session only once new-session's output has been parsed and validated into a typed session ID (spec §6.6).**
  - internal/fleet/fleet.go:189 obtains `createdSession` from Client.NewSession, which returns a value only after all four fields parse and validate (internal/tmuxx/tmux.go:137-159). Every rollback call site (fleet.go:197, 200, 207, 210) passes `createdSession.SessionID` and nothing else; there is no other producer of that value in Launch.
- **"Launch rollback kills only a session created by the current invocation" — and no failure path can kill the wrong session.**
  - fleet.go:323-330 is the only rollback; it calls l.tmux.KillSession(ctx, sessionID) with the ID from the creation record. internal/tmuxx/tmux.go:348-354 re-validates the '$' prefix before issuing `kill-session -t $ID`, so no name can reach -t. Launch never adopts a pre-existing session: the ListSessions pre-check only ever returns SessionExistsError (fleet.go:181-187), never a session to act on, and if ListSessions fails it falls through to new-session, which tmux 3.7b refuses on a duplicate name (measured: exit 1, "duplicate session: NAME"). Failure-by-failure in Launch: preflight/directory/ListSessions-context (fleet.go:167-180) — nothing created, nothing killed; new-session non-parse failure (fleet.go:194) — no ID, nothing killed; new-session malformed output (fleet.go:191-193) — no ID, nothing killed, CreationError raised; stampSession, stampWindow, and every later newWindow/stampWindow (fleet.go:196-211) — rollback kills the typed session ID this invocation created.
- **CreationError's text tells the truth about what is left behind.**
  - internal/fleet/fleet.go:51-53 renders "CAUSE; a session named S may exist; inspect with tmux ls", which matches spec §6.6's pre-ownership message verbatim. It is reached only when new-session exited 0 but its record failed to parse (tmux.go:139-153 wrap those in ErrCreationOutput; fleet.go:191). "may exist" is accurate rather than understated: new-session exiting 0 means tmux created the session, but a window command that exits immediately tears it down again. cmd/agentctl/main.go:426-453 maps it past the LaunchError branch to the exit-6 tmux class, as §6.6 requires.
- **"agentctl relaunch rolls back with tmux kill-window against only the window ID that invocation just created and parsed; it is never issued against a window found by listing."**
  - internal/fleet/relaunch.go:515-522 is the only rollbackWindow, and its two call sites (relaunch.go:259 and :264) both pass `created.WindowID`, produced by Client.NewWindow (internal/tmuxx/tmux.go:175-189) from the -P -F record and validated as '@'+digits at tmux.go:179. The windows obtained by listing in requireAbsentWindow (relaunch.go:433-450) flow only into WindowPresentError's message (relaunch.go:142-153) and observeWindow's read-only state probe (relaunch.go:470-502); no listed window ID reaches any kill.
- **"the wrapper takes a typed window ID so no name can reach -t" — and every kill wrapper validates its prefix.**
  - internal/tmuxx/tmux.go:359-365 KillWindow validates '@' before `kill-window -t`; tmux.go:348-354 KillSession validates '$' before `kill-session -t`. Both take typed Go types (WindowID/SessionID), not strings. Grep confirms exactly two non-test call sites in the whole tree: internal/fleet/fleet.go:328 (KillSession) and internal/fleet/relaunch.go:520 (KillWindow), plus the interface method in internal/kill/executor.go:14 used at executor.go:62.
- **"agentctl kill refuses sessions not marked agentctl-managed" — the managed+version gate precedes the kill and the kill is by session ID.**
  - internal/kill/executor.go:46-60 reads @agentctl_managed then @agentctl_version and returns RefusalError unless both equal "1"; only then does executor.go:62 call KillSession(ctx, target.ID). The ID comes from the session resolver's exact-name comparison over list-sessions with ambiguity refused (internal/session/resolver.go:127-141), so it is tmux's own $ID, and tmux.go:349 re-validates it.
- **No index panic is reachable in relaunch's stored-configuration lookup.**
  - internal/fleet/relaunch.go:380 indexes storedRoles with roleIndex(), which returns -1 when absent. It is unreachable: relaunch.go:330 has already refused unless the roster contains request.Role, and relaunch.go:365-370 has already refused unless sameRoles(storedRoles, rosterRoles) holds name-for-name in order (relaunch.go:579-589). The legacy branch returns earlier at relaunch.go:345.
- **agentctl never invokes a shell on this cluster's paths.**
  - internal/tmuxx/runner.go:30-32 and :36-51 use exec.CommandContext with argv arrays; there is no exec.Command("sh", ...) anywhere. The three send-keys calls and both kill calls all go through this Runner.

## metadata trust (SECURITY.md residual 4) — @agentctl_* options as untrusted input

- **Complete enumeration: every non-test read of an @agentctl_* option goes through exactly two accessors — Client.ShowSessionOption (show-options -qv) and Client.ListWindows (list-windows -F with the 9-field format). There is no non-test call of ShowWindowOption at all, so no per-window option read loop exists.**
  - internal/tmuxx/tmux.go:236 (ShowSessionOption), tmux.go:248 (ShowWindowOption, zero non-test callers — grep for ShowWindowOption returns only its definition), tmux.go:260 (ListWindows), tmux.go:15 (windowFormat). Call sites: internal/target/resolver.go:44,51,58; internal/fleet/relaunch.go:304,311,322,334,338,433; internal/status/collector.go:109,113,127,140,172; internal/kill/executor.go:46,54; internal/attach/executor.go:95,103,112.
- **The managed/version gate is exact string equality against "1" in all four commands, so every hostile variant I was asked about — absent/empty, "1 " with a trailing space, a very long string, embedded tabs, ANSI escapes, "..", a number, invalid UTF-8 — refuses cleanly rather than proceeding. No trimming, no prefix match, no numeric parse.**
  - internal/target/resolver.go:48 and :55 (managed != "1", version != "1"); internal/fleet/relaunch.go:308 and :315; internal/kill/executor.go:50 and :58; internal/attach/executor.go:99 and :107. The refusal messages render the observed value with %q (kill/executor.go:31, attach/executor.go:59, relaunch.go:73, cmd/agentctl/main.go:677), which escapes control bytes and invalid UTF-8 rather than emitting them raw.
- **No reachable panic exists in the metadata paths. Every index/slice access I was asked to check is guarded.**
  - decodeFleet: strings.SplitN(entry, ":", 4) at internal/fleet/relaunch.go:549 is followed by the len(fields) != 4 guard at :550 before fields[0..3] are read, and SplitN can never return more than 4. roleIndex(...) at relaunch.go:380 cannot return -1 there: containsRole (relaunch.go:330) proved request.Role is in rosterRoles, and sameRoles (relaunch.go:365, definition :579) proved storedRoles[i].Name == rosterRoles[i] element-wise after a length check, so the name is present in storedRoles. sameRoles itself compares lengths at :580 before indexing roster[index]. replaceRole guards with index >= 0 at :611. observeWindows indexes matches[0] at :461 only after observeWindows is called with len(matches) > 0 (relaunch.go:443-446) and after the len > 1 branch returns at :454. status/collector.go indexes matches[0] at :163 only after the 0 and >1 branches, and panes[0] at :195/:200 only after the len(panes) == 0 branch at :190. target/resolver.go indexes panes[0] at :84 only after len(panes) != 1 refuses at :81. tmuxx ListWindows indexes fields[0..8] only after len(fields) != 9 refuses (tmux.go:276).
- **CollectAll's error-type demultiplexing has no nil dereference, despite reading versionError.Version in a condition that can be reached with versionError nil.**
  - internal/status/collector.go:89 returns early unless at least one of versionError/rosterError matched, and line 93's `rosterError != nil || versionError.Version == ""` short-circuits on the left operand, so versionError is dereferenced only when rosterError is nil, which implies versionError is non-nil.
- **relaunch re-validates every stored @agentctl_fleet field on read exactly as SECURITY.md residual 4 promises: unknown harness, flag-smuggling model, and unknown effort are all refused rather than executed.**
  - internal/fleet/relaunch.go:553 (config.ValidateRoleName on field 0), :560 (config.ParseHarness on field 1 — closed enum, internal/config/config.go:137), :565 (config.ValidateModelName on field 2 — ^[a-zA-Z0-9][a-zA-Z0-9._-]*$, so a leading '-' cannot enter the model slot), :570 (config.ValidateEffort on field 3 against the per-harness allowlist). Structural failures become a MetadataError at :360 rather than a launch.
- **No stored metadata value can carry a newline into the re-encoded @agentctl_fleet, because Go's regexp `$` anchors at end-of-text (not before a trailing newline). I verified this empirically rather than assuming PCRE semantics.**
  - Ran ^[a-z0-9][a-z0-9_-]*$ and ^[a-zA-Z0-9][a-zA-Z0-9._-]*$ against "planner\n", "planner\nx", "planner\t", "fable\n": all false. Combined with decodeFleet's validation (relaunch.go:553-573) and EncodeFleet (internal/fleet/fleet.go:122-128), the write-back at relaunch.go:263 can only emit charset-bound fields.
- **Targeting never rests on a metadata VALUE in the ordinary case: the session ID comes from list-sessions with an exact name compare, and the window/pane IDs come from the ID field of tmux's own records. kill targets only the resolved session ID and reads no window metadata at all.**
  - internal/session/resolver.go:131-148 (resolveName, exact compare over ListSessions, no @agentctl_* read); internal/kill/executor.go:62 (KillSession by target.ID); internal/target/resolver.go:67 (windowIDs taken from window.ID), :77 (ListPanes by window.ID), :119 (returns pane.ID); internal/tmuxx/tmux.go:279 validates the ID field as '@' + digits before use. The one exception is Finding 1 below.
- **status's session-level gate matches spec §6.3's table row for row, including the deliberate ordering that makes a foreign version fail closed even on an unmanaged session.**
  - internal/status/collector.go:117 checks `version != "" && version != "1"` BEFORE the managed check at :120, so `managed missing + version 2` exits 3 (spec line 276 row 2) while `managed missing + version absent/1` renders managed:false (row 1). collector.go:124 covers managed=1 with an absent version; :131 covers an absent roster; :134-139 covers an empty roster entry, each with the message shape the spec requires.
- **attach's advisory window count degrades safely when ListWindows fails on malformed metadata: the count is omitted rather than guessed, and the count read cannot cause the attach to proceed or refuse incorrectly.**
  - internal/attach/executor.go:111-115 sets windowCount = -1 on countErr and writeNarration (executor.go:149) omits the count for a negative value. The gate at :99/:107 has already passed by then and does not consult the window list.
- **SECURITY.md's residual-4 wording is precise about what relaunch re-validates. It enumerates "harnesses, models and effort levels" and does not claim charset re-validation of @agentctl_dir; the directory receives exactly the check launch applies to an operator-supplied --dir (a stat for IsDir), and it reaches tmux only as a `-c` argv element, never a shell-interpreted string.**
  - SECURITY.md:45 (enumeration), internal/fleet/relaunch.go:504-513 (checkRelaunchDirectory: l.stat + IsDir, else StoredDirectoryError), internal/fleet/fleet.go:255-269 compared against internal/tmuxx/tmux.go:168 (`-c` as its own argv element). The one shell-interpreted value remains the window command at fleet.go:264.
- **An accidental (non-crafted) newline in a window option value fails closed with a parse error rather than being silently absorbed.**
  - A trailing newline fragment produces a second record with fewer than 9 fields, which internal/tmuxx/tmux.go:276-278 rejects with "parse tmux window record N: expected 9 fields and a nonempty name". Only a fully well-formed 9-field crafted line survives (see Finding 1).

## concurrency

- **Race 1 (two `agentctl launch --session S`): the loser is fail-noisy and damages nothing. It exits 6 carrying tmux's own "duplicate session: S" stderr, and never kills or touches the winner's session.**
  - Interleaving: both invocations run ListSessions before either runs new-session, so both pass the pre-check at /Users/rob/git/agentctl/.worktrees/issue-81-security-review/internal/fleet/fleet.go:181-187. The loser then calls newSession (fleet.go:189); tmux exits 1 (empirically verified this machine, tmux 3.7b: `duplicate session: fleet`, exit 1, session list unchanged). The error is not ErrCreationOutput, so Launch returns it raw (fleet.go:191-195) — crucially BEFORE any session ID exists, so no rollback path (fleet.go:323-330) can run and the loser has no session ID to kill. launchResult matches none of DirectoryError/MissingExecutableError/SessionExistsError/LaunchError and falls through to `tmuxx.ClassifyError(err)` + exitTmux at /Users/rob/git/agentctl/.worktrees/issue-81-security-review/cmd/agentctl/main.go:451-452. TmuxError.Error() appends the captured stderr (/Users/rob/git/agentctl/.worktrees/issue-81-security-review/internal/tmuxx/error.go:15-25). Exact behaviour is already pinned by tests: /Users/rob/git/agentctl/.worktrees/issue-81-security-review/cmd/agentctl/main_launch_test.go:231 asserts `agentctl: tmux create session: exit status 23: duplicate session: fleet` plus assertNoKillCall, and /Users/rob/git/agentctl/.worktrees/issue-81-security-review/cmd/agentctl/integration_launch_test.go:205 asserts exit 6 with the sentinel session byte-identical afterwards.
- **Race 1, exit-code divergence is real but intentional and tested: the pre-check path exits 3, the raced path exits 6, for the same real-world condition.**
  - Pre-check: /Users/rob/git/agentctl/.worktrees/issue-81-security-review/internal/fleet/fleet.go:184 returns SessionExistsError -> exit 3 at /Users/rob/git/agentctl/.worktrees/issue-81-security-review/cmd/agentctl/main.go:440-444 (message `session "fleet" already exists`, asserted at main_launch_test.go:180 and integration_launch_test.go:182). Raced path: exit 6 (main.go:451-452). The design spec anticipates exactly this fall-through at /Users/rob/git/agentctl/.worktrees/issue-81-security-review/docs/superpowers/specs/2026-08-01-agentctl-design.md:705 ("a check-to-create race still needs the identical duplicate-at-new-session fall-through"). Both codes are honest about what was observed; nothing is created or destroyed on either path.
- **Race 2 (launch between new-session and stampSession vs. a concurrent kill/clear/compact/relaunch): every mutating command fails closed at the ownership gate, and nothing destructive happens. `show-options -qv` on the unstamped session returns exit 0 with empty output, which reads as "unmanaged".**
  - kill: /Users/rob/git/agentctl/.worktrees/issue-81-security-review/internal/kill/executor.go:46-52 -> managed != "1" -> RefusalError -> exit 3 (main.go:455-460), KillSession never reached. clear/compact: /Users/rob/git/agentctl/.worktrees/issue-81-security-review/internal/target/resolver.go:44-50 -> SessionMetadataError -> exit 3 (main.go:675-679), no send-keys issued because Dispatcher resolves before delivering (/Users/rob/git/agentctl/.worktrees/issue-81-security-review/internal/control/dispatcher.go:37-42). relaunch: /Users/rob/git/agentctl/.worktrees/issue-81-security-review/internal/fleet/relaunch.go:304-310 -> SessionGateError -> exit 3 (main.go:640-649).
- **Race 2, status side: a mid-launch unstamped session is reported as `unmanaged` with exit 0, which is a factual report of what was observed rather than a false claim.**
  - /Users/rob/git/agentctl/.worktrees/issue-81-security-review/internal/status/collector.go:109-123: managed=="" and version=="" -> report.Managed=false, nil error; rendered as the `unmanaged` state at /Users/rob/git/agentctl/.worktrees/issue-81-security-review/internal/status/render.go:35-39. No mutation, exit 0.
- **Race 3 (`clear ROLE` vs `kill` on the same session): fail-safe in every interleaving. A payload can never land in a pane other than the one validated, and the worst outcome is a noisy exit 6 with the payload unexecuted.**
  - tmux never reuses pane IDs (empirically verified this machine), and delivery targets the exact resolved pane ID (/Users/rob/git/agentctl/.worktrees/issue-81-security-review/internal/tmuxx/control.go:19-39, validated at control.go:20). Interleavings: (a) session killed before the gate -> ShowSessionOption returns empty, exit 0 -> exit 3 refusal; (b) killed before ListWindows/ListPanes -> tmux exits 1 `can't find session` -> TmuxError -> exit 6 (resolver.go:58-80, main.go:731-735); (c) killed during the 1s payloadDelay between the literal payload and Enter (control.go:30-38) -> `send-keys -t %GONE` exits 1 `can't find pane` -> exit 6, payload never submitted. No interleaving delivers keys to a different agent.
- **Race 4 downstream: once two windows share a role name, every agentctl operation on that role fails closed rather than picking one.**
  - Control commands: /Users/rob/git/agentctl/.worktrees/issue-81-security-review/internal/target/resolver.go:70-72 (len(matches) != 1 -> RoleResolutionError) -> exit 4 naming both window IDs (/Users/rob/git/agentctl/.worktrees/issue-81-security-review/cmd/agentctl/main.go:685-691). status: both rows rendered `ambiguous` (/Users/rob/git/agentctl/.worktrees/issue-81-security-review/internal/status/collector.go:155-161). relaunch: refuses with WindowPresentError listing both as ambiguous (/Users/rob/git/agentctl/.worktrees/issue-81-security-review/internal/fleet/relaunch.go:443-450, 453-459).
- **SECURITY.md's rollback claims hold under concurrency: relaunch's cleanup only ever targets the window ID that invocation just created and parsed, and launch's rollback only a session it created.**
  - /Users/rob/git/agentctl/.worktrees/issue-81-security-review/internal/fleet/relaunch.go:515-522 passes only `created.WindowID` (obtained at relaunch.go:251) to KillWindow, which validates the typed @-ID (/Users/rob/git/agentctl/.worktrees/issue-81-security-review/internal/tmuxx/tmux.go:359-365). Launch rollback likewise uses only `createdSession.SessionID` (/Users/rob/git/agentctl/.worktrees/issue-81-security-review/internal/fleet/fleet.go:196-211, 323-330). Neither can name another invocation's object because no name-based lookup feeds either call.
- **Every tmux object agentctl addresses is an ID that tmux never recycles, so no race can redirect a `-t` at a newer object.**
  - Empirically verified this machine (tmux 3.7b): after killing $0, the next session created is $2, not $0; window/pane IDs likewise (established by the lead reviewer). All targeting goes through typed IDs validated at /Users/rob/git/agentctl/.worktrees/issue-81-security-review/internal/tmuxx/tmux.go:427-437.
- **Race 6: there is no locking of any kind — no file lock, no tmux advisory option, no serialization primitive.**
  - `grep -rn 'flock|sync.Mutex|LOCK_EX|O_EXCL|lockfile' --include='*.go'` over the tree returns exactly one hit, /Users/rob/git/agentctl/.worktrees/issue-81-security-review/internal/tmuxx/runner.go:70, which is the test-only FakeRunner's mutex. Every check-then-act in the product (fleet.go:174-189, relaunch.go:241-251, relaunch.go:334+261-266, kill/executor.go:46-62, target chain -> control.go:19) is therefore non-atomic across processes.

## Spec conformance and the remaining SECURITY.md claims (§7, §8, §12, §13, §6.5/§6.6; residuals 1, 5; file/socket claim; release-checklist claim)

- **§13.2's canonical tmux argv table is implemented element-for-element, with no divergence in either direction: all 16 rows exist and no operation outside the table is exposed.**
  - Row 1 internal/tmuxx/tmux.go:104; row 2 tmux.go:130-132; row 3 tmux.go:168-170; row 4 tmux.go:213; row 5 tmux.go:231; row 5a tmux.go:222; row 6 tmux.go:240; row 7 tmux.go:252; row 8 tmux.go:264; row 9 tmux.go:302; row 10 internal/tmuxx/control.go:23,26,38; row 11 tmux.go:352; row 11a tmux.go:363; row 12 tmux.go:372; row 13 tmux.go:388; row 14 control.go:48. Flag order, the `-w`/`-q`/`-v` combinations, `-P -F` placement and the `--` before CMD all match §13.2/§13.3 exactly. The Client exposes exactly these 16 operations and no SendKeys wrapper (§13.6).
- **Every -t target is a typed ID validated before the argv is built; no name can reach -t.**
  - validateID at internal/tmuxx/tmux.go:427-437 is called at the head of tmux.go:165,210,218,228,236,248,261,299,349,359,369,385 and control.go:20. Even row 12's `$TMUX_PANE` is validated as `%N` (tmux.go:369) before it is passed, which is stricter than §13.2 row 12 requires.
- **§13.3 format strings and their parsers match the spec byte-for-byte, including the 9-field window collection format and SplitN(…, 9).**
  - internal/tmuxx/tmux.go:12-16 (sessionFormat, createdSessionFormat, createdWindowFormat, windowFormat, paneFormat); SplitN 9 at tmux.go:275; `@agentctl_process` is last, so a delimiter inside it cannot shift another field.
- **agentctl never invokes a shell; all external execution is argv arrays through one Runner.**
  - internal/tmuxx/runner.go:31 and :37 use exec.CommandContext with (executable, args...) only. Grep for runner use in non-test code returns only internal/tmuxx/{tmux,control}.go and the wiring at cmd/agentctl/main.go:72.
- **§6.5 metadata stamping order is implemented exactly as specified and is asserted as an ordered call sequence, not a set.**
  - Session order (managed, version, roles, fleet, dir) at internal/fleet/fleet.go:268-284; window order (managed, role, harness, model, effort, baseline poll, process) at fleet.go:288-303. The assertion helper compares the complete ordered call slice with reflect.DeepEqual: internal/fleet/fleet_test.go:565-570, used at fleet_test.go:277-300 and 330-366; relaunch asserts the same ordered sequence at internal/fleet/relaunch_test.go:96-113. Reordering any stamp fails these tests.
- **Row 5a is issued only by launch, once per variable, after the first window is stamped and before any later window; its failure is reported on stderr and never rolls back.**
  - internal/fleet/fleet.go:202 calls clearSessionIdentity after stampWindow of the first role and before the loop at fleet.go:204; clearSessionIdentity (fleet.go:216-222) writes to stderr and returns nothing. Relaunch never calls it (internal/fleet/relaunch.go:251 uses newWindow only), which is correct because new-window does not write the session environment.
- **§8's poll parameters (5s timeout, 100ms cadence, attempt at t=0, guaranteed boundary attempt before declaring timeout) are implemented as stated, and the two documented retry conditions are exactly the two implemented.**
  - internal/fleet/fleet.go:97-98 (5s / 100ms); fleet.go:308-320 attempts before checking the deadline, so the boundary attempt always precedes the timeout; fleet.go:310 accepts a non-`amq` value, fleet.go:313 retries only ErrProcessUnavailable, matching spec lines 796-797.
- **§7's validation rules are implemented as written: name and model charsets, per-harness effort allowlist, harness set, and --dir must be an existing directory evaluated before any tmux call.**
  - internal/config/config.go:11-12 (namePattern, modelPattern), config.go:36-38 (effort allowlists), Harness constants config.go:23-25; directory check internal/fleet/fleet.go:332-346 runs before ListSessions (fleet.go:170 precedes fleet.go:174).
- **Residual 5 is accurate: AGENTCTL_ROLE and AGENTCTL_MANAGED are never read anywhere in non-test code, and AGENTCTL_SESSION is read at exactly one place, as a session-selection source.**
  - Only non-test reads are internal/session/resolver.go:107 (`lookupEnv("AGENTCTL_SESSION")`); the names otherwise appear only as write-side constants at internal/fleet/fleet.go:99-101,217,250-252. No target/kill/attach/status validation reads them: internal/target/resolver.go:44-118 and internal/attach/executor.go:95-119 read only @agentctl_* options.
- **Residual 5's carve-out is also complete for status: an ambient AGENTCTL_SESSION cannot narrow a bare `status` listing.**
  - cmd/agentctl/main.go:244 short-circuits to statusAll before the resolver is consulted when --session was not set, matching spec §4.1 line 168.
- **SECURITY.md's "agentctl creates no persistent files of its own (no database, no state directory)" is true of agentctl's own process.**
  - Grep across all non-test Go for os.Create/os.WriteFile/os.OpenFile/os.MkdirTemp/os.CreateTemp/os.Mkdir/os.Remove/os.Rename/ioutil returns zero hits. The only os calls are os.Getwd/os.Stat/os.LookupEnv/os.Std* (cmd/agentctl/main.go:68,72; internal/fleet/fleet.go:136,139; internal/tmuxx/runner.go:40,44,48).
- **The payload registry is closed and argument-free, and delivery is exposed only as the composite operation.**
  - internal/control/registry.go:17-21 holds two complete constants and Lookup exposes no interpolation point; internal/tmuxx/control.go:19-40 is the only send-keys path and the delay is the package constant payloadDelay (control.go:12), not a parameter.
- **internal/config's effort pin ("verified 2026-08-03 against Claude Code 2.1.220: low, medium, high, xhigh, max") still holds on the current Claude Code 2.1.222.**
  - internal/config/config.go:31-38 lists the five levels; `claude --version` reports 2.1.222 (Claude Code) and `claude --help` prints `--effort <level>  Effort level for the current session (low, medium, high, xhigh, max)`. No re-pin is needed; no agent or fleet was launched to establish this.
- **Launch rollback and relaunch rollback target only what the invocation created, by typed ID.**
  - internal/fleet/fleet.go:323-330 passes createdSession.SessionID to KillSession; internal/fleet/relaunch.go:515-522 passes the parsed created.WindowID to KillWindow; both wrappers re-validate the ID prefix (tmux.go:349,359).
- **Row 7 (ShowWindowOption) is implemented and spec-conformant but has no production caller; window metadata is read through the row 8 format string instead.**
  - internal/tmuxx/tmux.go:248 is referenced only from internal/tmuxx/tmux_test.go; all window-metadata reads go through ListWindows (internal/target/resolver.go:58, internal/status/collector.go:140, internal/fleet/relaunch.go:433). Not a defect — §13 lists the row — but it is exported surface with no consumer.
- **The full unit suite passes on this branch.**
  - `go test ./...` in /Users/rob/git/agentctl/.worktrees/issue-81-security-review: ok for all 16 packages.

## regression-test coverage of the security invariants

- **Invariant 1 (PINNED): shellq.Quote is total and round-trips through a real POSIX shell.**
  - internal/shellq/shellq_test.go:42 TestQuoteRoundTrip (8 cases incl. lone single quote, backslash, embedded newline, shell operators) and internal/shellq/shellq_test.go:95 FuzzQuoteRoundTrip (12 seeds), both via internal/shellq/shellq_test.go:118 assertQuoteRoundTrip, which executes `sh -c "set -- <Quote(input)>; printf '%s:' \"$#\"; printf %s \"$1\""` (line 124-128) and asserts exactly one word equal to the input. NUL is explicitly skipped (line 120-122), matching the documented exception at internal/shellq/shellq.go:3-4. Exact-output table at internal/shellq/shellq_test.go:10 TestQuote; Join at :67.
- **Invariant 2a (PINNED): the window command reaches tmux as ONE argv element after a literal "--", on both creation paths.**
  - internal/tmuxx/tmux_test.go:76-90 TestNewSession... and :113-125 TestNewWindow... assert the full argv ending `"-P","-F",<format>,"--","exec amq coop exec"`. Wired end-to-end with real quoting at internal/fleet/fleet_test.go:282-284 (`"--", "exec 'amq' 'coop' 'exec' '--session' 'epic123' '--me' 'planner' 'claude' '--' '--model' 'fable'"`), :335-336, :356-357, :411-412, :432-433, internal/fleet/relaunch_test.go:102-103, :132-133, and at CLI level cmd/agentctl/main_launch_test.go:341 and :357. The directory (`-c dir`) and env (`-e NAME=value`) are separate argv elements, never concatenated into the shell string (internal/tmuxx/tmux.go:130-132, :193-202).
- **Invariant 2b (PINNED, indirectly): agentctl never invokes a shell.**
  - internal/tmuxx/runner.go:31 and :37 use exec.CommandContext(ctx, executable, args...) with argv arrays. Every unit test asserts against tmuxx.FakeRunner Calls recording {Executable, Args} element-by-element (internal/tmuxx/runner.go:88-103); the integration suite's socketRunner does the same against a real tmux binary (cmd/agentctl/integration_fixture_test.go:105-138).
- **Invariant 3 (PINNED): send-keys argv exactness — C-u, then -l -- payload, then Enter, as three separate calls in that order.**
  - internal/tmuxx/control_test.go:10 TestDeliverPayloadUsesFixedThreeCallSequence asserts reflect.DeepEqual over the exact three-Call slice (lines 17-24). Ordering under failure is pinned at :27 TestDeliverPayloadStopsOnFirstError (stops after call 1, 2 or 3), cancellation-before-Enter at :84 TestDeliverPayloadCancellationPreventsEnter, and name-target refusal before any tmux call at :72 TestDeliverPayloadRejectsNameTargetBeforeRunningTmux. Repeated at CLI level in cmd/agentctl/main_control_test.go:343.
- **Invariant 4 (PINNED): the payload registry is closed, payloads are constants, and an unknown operation is refused before any dependency runs.**
  - internal/control/registry_test.go:10 TestOperationsExposeOnlyRatifiedArgumentFreePayloads pins the exact two-entry registry and asserts no '%' formatting verb and no whitespace in any payload (lines 19-26). Refusal: internal/control/registry_test.go:29 TestLookupRejectsUnknownOperationAsDefenseInDepth and internal/control/dispatcher_test.go:52 TestDispatcherUnknownOperationIsInternalDefenseInDepthBeforeDependencies (asserts neither resolver nor deliverer was called). CLI-level closure of the caller-text path: cmd/agentctl/main_control_test.go:64 TestRunControlRejectsCallerPayloadInputsBeforeDependencies rejects extra positional text, --text, --command=/rename, --raw=/rename and --keys=C-u with exitUsage and zero dependency calls.
- **Invariant 5 (PINNED, all eleven links): the fail-closed target chain.**
  - internal/target/resolver_test.go — role charset before any command: :17 TestResolverRejectsMalformedRoleBeforeClientCall (zero runner calls). Unmanaged session and wrong version: :33 TestResolverRejectsInvalidSessionMetadataAtFirstFailedGate, subtests 'managed unset', 'managed wrong', 'version unset', 'version wrong', each asserting the exact call prefix. Zero matching windows and multiple matching windows: :128 TestResolverRequiresExactlyOneExactWindowName, subtests 'no exact match despite prefix and suffix decoys' (wantIDs empty) and 'multiple exact matches' (@4, @7). Unmanaged window and stored-role mismatch: :192 TestResolverRejectsFirstInvalidWindowMetadataField (3 calls, no pane probe). Multiple panes and dead pane: :239 TestResolverRejectsUnsafePaneStateBeforeProcessProbe, subtests 'zero panes', 'multiple panes', 'sole record reports multiple window panes', 'sole pane dead', each asserting no ps and no delivery. Empty @agentctl_process: :303 TestResolverRejectsEmptyProcessBaselineBeforeProcessProbe (lookupEnv fails the test if called). Process mismatch: :365 TestResolverRejectsProcessIdentityMismatchBeforeSelfTargetCheck (expected codex, actual zsh). Self-target: :431 TestResolverRejectsCallingPaneAfterIdentitySucceeds (TMUX_PANE=%8 equals target). Success path with exact argv and no delivery: :388 TestResolverReturnsPaneWhenCallerIsUnsetOrDifferent.
- **Invariant 6 (PINNED): kill refuses an unmanaged/wrong-version session and otherwise targets by session ID.**
  - internal/kill/executor_test.go:12 TestExecuteKillsOnlyAfterManagedVersionGate asserts the exact three-call sequence ending `kill-session -t $4`. Refusals: :31 TestExecuteRefusesEveryFailedOwnershipGateWithoutKilling (managed missing/wrong, version missing/wrong) with exact call lists proving no kill. ID-only targeting is enforced at the wrapper: internal/tmuxx/tmux_test.go:293 TestTypedTargetsRejectNamesBeforeRunningTmux subtest 'kill' (KillSession("epic123") errors with zero external commands) and :341 TestKillSessionUsesResolvedID. CLI level: cmd/agentctl/main_test.go:263 and :292.
- **Invariant 7a (PINNED): relaunch rollback kills only the window that invocation created, and never a session.**
  - internal/fleet/relaunch_test.go:653 TestRelaunchRollsBackOnlyTheWindowThisInvocationCreated injects a failure at each of the 8 post-creation steps, asserts exactly index+2 calls, asserts the last call is exactly `kill-window -t @71` (the parsed created ID), and scans every call for kill-session (lines 693-697). Pre-ownership failures remove nothing: :632 TestRelaunchMalformedCreationOutputRemovesNothing via assertNoWindowKill (:863). Typed-ID-only kill-window: internal/tmuxx/tmux_test.go:293 subtest 'kill window' and :351 TestKillWindowUsesResolvedID.
- **Invariant 7b (PINNED): relaunch refuses when a role window exists in ANY state — running, dead, unmanaged (metadata), unmanaged (pane count), pane-less/missing, unexpected-process, ambiguous.**
  - internal/fleet/relaunch_test.go:465 TestRelaunchRefusesAnyExistingRoleWindowRenderingObservedState covers all seven subtests ('running', 'dead', 'unmanaged metadata', 'unmanaged pane count', 'zero panes'→missing, 'unexpected process', 'ambiguous'), asserts the exact refusal message including the observed state, and calls assertNoCreation (:853), which scans for new-window/new-session AND kill calls. Real tmux equivalent: cmd/agentctl/integration_relaunch_test.go:40-43 (live role refused with exitRole and 'already has 1 window') and :56-74 (after killing the window, relaunch succeeds with a NEW window ID).
- **Invariant 8 (PINNED): decodeFleet re-validates harness, model and effort read out of @agentctl_fleet and refuses defective metadata.**
  - internal/fleet/relaunch_test.go:324 TestRelaunchRefusesDefectiveFleetMetadata — 9 subtests including 'unknown harness' (planner:bash::), 'smuggled model' (planner:claude:--dangerously:), 'unknown effort' (planner:claude::turbo), 'entry is not a quad', 'duplicate role', roster/fleet disagreement and ordering, each asserting the exact message and assertNoCreation. Implementation at internal/fleet/relaunch.go:544-577. Roster defects: :272 TestRelaunchRefusesDefectiveRoster; role outside roster: :304; legacy session refusal with no directory defaulting: :392 and :426.
- **Invariant 9 (PINNED): charset rejection for model, effort, session and role, including trailing-newline and flag-shaped values.**
  - Model: internal/config/config_test.go:373 TestValidateModelNameEnforcesCharsetIncludingEmptyAndFlagSmuggling (empty, --dangerously-bypass-approvals-and-sandbox, space, colon, comma) plus :204 TestParseFleetRejectsInvalidModelLists subtests 'trailing newline in model' (:229), 'newline flag smuggling' (:230), 'flag smuggling' (:231), 'tab in model' (:228). Effort: :328 TestParseFleetRejectsInvalidEffortLists subtests 'trailing newline level' (:357), 'flag smuggling' (:358), 'toml string escape' (:359 — `p:high"\nmodel="evil`, the codex --config injection case). Session: :24 TestValidateSessionNameRejectsInvalidNames including 'trailing newline' (:35), 'tab', 'carriage return', 'NUL', 'leading hyphen'. Role: :78 TestValidateRoleNameRejectsInvalidNames ("", Planner, -planner, plan/ner, plan\nner) and :142 'newline in role'. Harness: :401 TestParseHarnessAcceptsOnlyRegisteredHarnesses (rejects "", bash, Claude, "claude ", "sh -c"). I confirmed independently that Go's regexp `$` is end-of-text (not end-of-line): `^[a-z0-9][a-z0-9_-]*$` and `^[a-zA-Z0-9][a-zA-Z0-9._-]*$` both reject "planner\n"/"fable\n".
- **Invariant 10 (PINNED): metadata stamping ORDER, not just the set.**
  - internal/fleet/fleet_test.go:255 TestLaunchOneRoleCreatesAndStampsReturnedIDs asserts the whole ordered call list via reflect.DeepEqual: list-sessions → new-session → @agentctl_managed, @agentctl_version, @agentctl_roles, @agentctl_fleet, @agentctl_dir (session, lines 285-289) → @agentctl_managed, @agentctl_role, @agentctl_harness, @agentctl_model, @agentctl_effort (window, lines 290-294) → `ps -o comm= -p 4242` → @agentctl_process LAST (lines 295-296) → the three set-environment -u calls. Relaunch mirrors it at internal/fleet/relaunch_test.go:61 TestRelaunchStoredConfigurationRecreatesWindowAndStampsInOrder (lines 105-111), and the fleet-metadata rewrite is pinned to happen AFTER the process baseline at :147 TestRelaunchFlagOverridesRewriteFleetMetadataAfterTheBaseline. Multi-role declaration order: internal/fleet/fleet_test.go:369.
- **The integration suite creates its own throwaway tmux server and never touches the default one.**
  - cmd/agentctl/integration_fixture_test.go:323-324 builds socket `agentctl-test-<8 random hex bytes>` and a private TMUX_TMPDIR under /tmp; :134-138 tmuxCommand prepends `-L <socket>` to EVERY tmux invocation and overrides TMUX_TMPDIR; :335-351 kill-server cleanup with a 5s deadline; :91 TestIntegrationFixtureRemovesSocketDirectory asserts the socket directory is gone afterwards. I verified after the run that no agentctl socket remains under /private/tmp/tmux-501.
- **`go test -tags integration ./...` currently passes on this machine (go1.26.5, tmux 3.7b).**
  - All 16 packages ok, no failures: cmd/agentctl 6.668s, hack 78.127s, internal/attach 6.029s, internal/buildinfo 0.758s, internal/cliflags 5.280s, internal/config 5.650s, internal/control 2.628s, internal/fleet 2.243s, internal/harness 3.011s, internal/kill 1.507s, internal/preflight 0.385s, internal/session 4.522s, internal/shellq 4.217s, internal/status 3.390s, internal/target 4.889s, internal/tmuxx 4.795s. `go test ./...` also passes (all packages ok/cached).
- **The integration suite gives real-tmux coverage of wrong-target delivery, ownership-gated kill, relaunch refusal/recreation, and argv element boundaries through tmux's shell interpretation of the window command.**
  - cmd/agentctl/integration_lifecycle_test.go:149 TestIntegrationClearReachesOnlyTargetMarker (planner receives "/clear\n"; coder's captured input asserted to REMAIN empty for 750ms at :167); :170 TestIntegrationKillRemovesManagedSession; cmd/agentctl/integration_relaunch_test.go:14 TestIntegrationRelaunchRecreatesOnlyTheAbsentRole; cmd/agentctl/integration_launch_test.go:30 (metadata + baseline), :129 (identity env in every pane), :172 (existing session refused without mutation), :217 (rollback after later-window failure). The amq stub at cmd/agentctl/integration_fixture_test.go:180-247 re-parses the argv it actually received after tmux ran the shell-interpreted `exec 'amq' ...` string, exiting 86 if the element boundaries or the `--` separator are wrong — so shellq's output is checked end-to-end against a real shell, not only against the fake runner.

## Candidate findings that did not survive refutation

Recorded so the same ground is not re-covered. Each was raised by an auditor
and then killed by an independent refuter.

- **"fuzz-tested" overstates the coverage CI actually provides: the fuzz target only ever replays 12 in-code seeds** (quoting; raised informational, verdict not-a-finding)
  - The finding's mechanical facts are all correct — I re-derived every one of them — but they do not add up to a defect.

Verified true:
- `FuzzQuoteRoundTrip` exists at /Users/rob/git/agentctl/.worktrees/issue-81-security-review/internal/shellq/shellq_test.go:95, with exactly 12 `f.Add` seeds at lines 96-111 ("", "plain", "'", "don't", `\`, `"double"`, "$HOME", "`whoami`", "two words", "line1\nline2", ";|&<>()", "--flag").
- The property helper at shellq_test.go:118-133 is genuine: it builds `set -- <Quote(input)>; printf '%s:' "$#"; printf %s "$1"`, executes it through real `sh` (line 125), and asserts both word count and byte equality against `1:` + input (lines 129-130). NUL is skipped (line 120-122), matching the documented exclusion at docs/superpowers/specs/2026-08-01-agentctl-design.md:886.
- There is no seed corpus on disk: `find . -name testdata -type d` returns only ./hack/testdata; internal/shellq contains only shellq.go and shellq_test.go.
- CI (.github/workflows/ci.yml, "Test" step) runs `go test ./...` and `go test -tags integration ./...`; `grep -rni fuzz` over the repo finds no `-fuzz` or `-fuzztime` invocation anywhere in .github/ or hack/.
- I confirmed the seeds do run under plain `go test`: `go test ./internal/shellq/ -run 'FuzzQuoteRoundTrip|TestQuote' -v` shows `FuzzQuoteRoundTrip/seed#0` … `seed#11` all PASS.

Why this is nevertheless not a finding:

1. No gap between what SECURITY.md promises and what the code does. The security property SECURITY.md:9 asserts is that the shell-interpreted window command "is assembled by a dedicated, fuzz-tested quoting layer from tokens that have already passed these checks." `Quote` at internal/shellq/shellq.go:10-12 is `"'" + strings.ReplaceAll(s, "'", `'"'"'`) + "'"` — unconditional, total single-quote wrapping with the canonical embedded-quote escape. It has no charset branch, no fast path, no precondition. The claimed property holds for every non-NUL input as written, and the original auditor confirms this (45s / 48,837 execs, PASS). SECURITY.md does not overstate anything about behaviour.

2. "fuzz-tested" is a true statement about the artifact. A checked-in `Fuzz*` target *is* a Go fuzz test; it is exercised in CI over its seed corpus on every run, and it has in fact been fuzzed. SECURITY.md nowhere claims continuous randomized coverage, and neither does the governing spec — docs/superpowers/specs/2026-08-01-agentctl-design.md:216 and :853 use precisely the same vocabulary ("table- and fuzz-tested", "table tests + Go fuzz test asserting the exactly-one-word round-trip property"). The doc language matches the approved contract and the artifact that exists. Reading "fuzz-tested" as "CI runs `go test -fuzz` on every push" is an interpretation the document does not invite, and essentially no Go project satisfies it (fuzzing is unbounded and needs an explicit `-fuzztime`).

3. The failure scenario is not reproducible from the code as written. It requires a hypothetical future maintainer to rewrite `Quote` with an `if allowed(s) { return s }` fast path *and* to edit both table tests to match. That is a speculative regression under a future refactor, not a defect present in the branch under audit. The brief is explicit that every finding must be reproducible from the code as written. Note also that such a refactor would have to defeat TestQuote (shellq_test.go:10-40), which pins exact output for "plain"/"agent123" and "--flag" — the very tokens the scenario relies on passing unquoted — so the change is not the silent one-line slip the scenario describes.

4. Nothing actionable follows. The original auditor set `needs code fix: false` and severity `informational`. What remains is a testing-strategy preference (add a corpus, or add a time-bounded `-fuzz` CI step) applied to a 3-line total function whose correctness is already pinned by 12 exact-output table cases, 8 real-`sh` round-trip cases, and 12 seed round-trips — all green in CI. A preference for stronger regression insurance on already-correct, already-tested code is not a security finding, and reporting it as a documentation overstatement would itself be the inaccurate claim.

Downgrading to not-a-finding rather than informational: an informational entry here would assert a promise/behaviour gap that I cannot demonstrate, since both the promise and the behaviour check out.
- **Relaunch validates --effort against Claude's level set regardless of the effective harness, so the "closed per-harness set" guarantee is enforced only by the two sets happening to be identical** (quoting; raised informational, verdict not-a-finding)
  - Code claims are accurate, but the finding is refuted because the hazard it describes is not reachable from the code as written and is already guarded by a test.

What I confirmed as literally true:
- internal/fleet/relaunch.go:224-231 validates request.Effort against config.HarnessClaude unless request.Harness is explicitly supplied; the stored harness is only read later, in resolveConfiguration at relaunch.go:236 onward (roster/fleet reads at relaunch.go:321-340, override assignment at relaunch.go:389-396).
- cmd/agentctl/main.go:573-579 (validateRelaunchOverrides) does the same defaulting.
- The value is then rendered by the role's real harness at internal/harness/harness.go:59-64 -> codexEffortArgs (internal/harness/harness.go:88-90), which emits --config model_reasoning_effort=%q.
- effortLevels for both harnesses are byte-identical at internal/config/config.go:36-39.

Why it does not stand as a finding:

1. No behaviour to reproduce, by the finding's own admission. With effortLevels[HarnessClaude] == effortLevels[HarnessCodex], every accepted value is one of five literal lowercase ASCII words, so both the acceptance decision and the rendered TOML expression are correct for either harness. The failure scenario requires a future source change, not the code as written.

2. The divergence premise is contradicted by an existing test. internal/config/config_test.go:304-315, TestEffortLevelsAreTheDocumentedSet, asserts EffortLevels(HarnessClaude) and EffortLevels(HarnessCodex) both equal exactly {low, medium, high, xhigh, max}. Widening either list fails that test by name, so the identical-sets invariant is enforced, not coincidental. internal/config/config.go:29-35 additionally records the intersection choice as deliberate design intent (codex's none/minimal/ultra are excluded because agentctl exposes only levels verified on both harnesses).

3. No gap between SECURITY.md and the code. SECURITY.md:9 states one concrete set for all harnesses ("effort levels against a closed per-harness set (low, medium, high, xhigh, max)") and the security consequence it draws ("no caller-supplied text reaches the codex --config expression") holds unconditionally on this path. SECURITY.md:12's allowlist-not-charset claim also holds. The relaunch bullet at SECURITY.md:16 says overrides may replace a field and are written back with reported provenance; it makes no per-effective-harness validation promise that the code breaks. Stored values are re-validated against their own stored harness at internal/fleet/relaunch.go:570, exactly as documented.

4. Not an accident, wrong-target delivery, crash, or doc/code gap - the four categories in scope.

At most this is a non-security maintainability note (the two validation sites would be tidier if they deferred effort validation until after resolveConfiguration, or if config exposed a single ValidateEffortAnyHarness). It should not appear in the security report as a finding.
- **relaunch --effort without --harness is validated against Claude's effort set, never against the harness that will actually run** (validation; raised low, verdict informational)
  - Every code claim checks out, but the finding does not survive as a security defect.

VERIFIED AS DESCRIBED: cmd/agentctl/main.go:573-581 and internal/fleet/relaunch.go:224-232 both hardcode `harness := config.HarnessClaude` and only replace it when --harness is also supplied; CLI validation at main.go:234-238 precedes session resolution at main.go:251; relaunch.go:391-394 sets role.Effort while leaving role.Harness at the stored value (so no wrong harness reaches argv); internal/harness/harness.go:93-95 re-validates nothing. I built the binary and confirmed the message: `agentctl relaunch --session sess --effort=turbo role` prints `invalid --effort value "turbo": harness "claude" does not support effort "turbo"; supported levels are low, medium, high, xhigh, max`.

WHY IT IS REFUTED:

(1) No gap between SECURITY.md and the code. SECURITY.md:9 promises effort levels are checked "against a closed per-harness set (`low`, `medium`, `high`, `xhigh`, `max`)" and SECURITY.md:10 promises allowlist-not-charset matching. internal/config/config.go:37-38 makes both lists byte-identical to that enumerated set, so the value reaching codexEffortArgs is always a member of the exact set the document names. The document makes no claim the code fails to honour, so there is nothing overstated to report under the "doc overstates its guarantees" clause.

(2) No reachable failure today. The finding itself concedes "no invalid value can get through and there is NO exploitable consequence now." The lockout chain (relaunch.go:261-266 write-back, then decodeFleet's ValidateEffort at relaunch.go:570 refusing every later relaunch) requires the two lists at config.go:37-38 to first be edited apart. That is a hypothetical future source change, not behaviour reproducible from the code as written.

(3) The claimed "false factual claim" is not false. "harness \"claude\" does not support effort \"turbo\"" is a true sentence, the refusal is the correct outcome for every stored harness, and the remediation advice it prints is correct for codex too because the lists match. No user is misdirected, no unobserved success is reported, no defect is hidden — so spec 1.1 is not violated. It is a wording imprecision (the message names a harness the session may not run).

(4) No same-user mechanism is even involved, so the scope exclusion is not what kills it — the absence of any present-day wrong outcome is.

RESIDUE: an informational note that the relaunch effort validator assumes Claude's list and that the two lists are load-bearingly coupled without a test asserting it. Worth a comment or a table-driven equality test if someone is already editing that file; not a low-severity security finding and not something that should reach the report as one.
- **Unreachable duplicate launch contract in parseCommand diverges from the real one** (validation; raised informational, verdict not-a-finding)
  - The structural observation is accurate but it is not a finding under this audit's scope, and its two load-bearing claims (test exercise, doc/code gap) are false.

VERIFIED AS DESCRIBED:
- cmd/agentctl/main.go:487-490 does contain a `case "launch"` declaring --roles, --models and a discarded --dir, with no --efforts.
- cmd/agentctl/main.go:516-531 is a second copy of the launch positional/required checks, including a second config.ValidateSessionName at main.go:520.
- The branch is unreachable: runWithAllDependencies returns for launch at main.go:200-219 before reaching parseCommand at main.go:221. grep confirms parseCommand has exactly one call site (main.go:221) and parseLaunch exactly one (main.go:201).

REFUTED:
1. "It is exercised only by tests" is false. No test calls parseCommand; grep over all *.go finds only the two production call sites. A coverage run over ./cmd/agentctl reports count 0 for every block in both launch branches (main.go:487.16,490.47 3 0; main.go:516.16,517.26 1 0; main.go:520.3,520.69 1 0; ... main.go:529.27,531.4 1 0). The code is executed by nothing, so it cannot be silently diverging under test pressure either.

2. The claimed doc-vs-code gap does not exist. Spec line 773 ("--efforts shares every structural rule with --models") describes internal/config validation, and the live path honors it exactly: parseLaunch declares --efforts at main.go:400 and handles set-ness symmetrically with --models at main.go:411-418, then main.go:212 calls config.ParseFleet(roles, models, efforts). The help text at main.go:52 therefore advertises an option the binary actually accepts. This is covered end to end by cmd/agentctl/main_launch_test.go:39-41 and :330, main_test.go:148-149 and :177-178, integration_launch_test.go:39, and internal/config/config_test.go:342-359. --efforts validation is reachable and tested; nothing in the shipped binary contradicts the spec.

3. The failure scenario is explicitly counterfactual — it requires "if the early dispatch at main.go:200 were ever refactored away". The finding itself concedes "No live defect: the branch cannot execute." A finding must be reproducible from the code as written; this one is reproducible only from code that does not exist.

4. Scope: the audit counts accidents, wrong-target delivery, crashes/panics, and gaps between SECURITY.md promises and code. Unreachable duplicated flag parsing is none of those. It is not a same-user-attacker case either — it is simply dead code.

Residual truth worth passing along outside the security report: main.go:487-490 and 516-531 are dead and should be deleted (the `case "launch"` arms in both switches), which would also let parseCommand stop importing the launch-only checks. That is structural debt for a cleanup PR, not a security finding.
- **SECURITY.md describes the validation chain as if it held at delivery time; it is evaluated once, and a pane that dies under remain-on-exit absorbs the payload while agentctl reports success** (targetchain; raised low, verdict not-a-finding)
  - REFUTED. The mechanical observations are correct, but the documentation gap the finding rests on does not exist.

What I confirmed as accurate in the finding:
- /Users/rob/git/agentctl/.worktrees/issue-81-security-review/internal/control/dispatcher.go:37 resolves and :41 delivers, with no re-validation between them.
- /Users/rob/git/agentctl/.worktrees/issue-81-security-review/internal/tmuxx/control.go:19-40 issues send-keys C-u (:23), send-keys -l -- payload (:26), a 1s timer (:12, :30-36), then send-keys Enter (:38).
- /Users/rob/git/agentctl/.worktrees/issue-81-security-review/cmd/agentctl/main.go:290 prints `agentctl: delivered %s to %s:%s` and returns exitOK.
- I reproduced the tmux behaviour myself on a throwaway socket (`tmux -L agentctl-refute-$$`, killed afterwards): with `remain-on-exit on` and the pane process SIGKILLed, the pane persisted as `%0 1 1` (id, pane_dead, window_panes) and all three send-keys calls exited 0.

Why it is nonetheless not a finding:

1. The claim quoted from SECURITY.md:12 is not violated. That bullet's promise is titled "Controlling the wrong terminal" and ends "Delivery targets the resolved pane ID." In the dead-pane case delivery does target the resolved pane ID, and no wrong terminal is controlled. The finding itself concedes the other two TOCTOU outcomes are clean (destroyed pane -> send-keys exit 1 -> fail-noisy; pane IDs are never reused). So the specific security property the cited line promises holds in every branch.

2. The "gap between doc and code" is closed explicitly, in three places, and the finding's characterisation of residual 1 as framed "entirely in terms of TUI state, autocomplete popups and host saturation" is wrong on its own text:
   - SECURITY.md:27 — the residual's *title* is "Keystroke delivery is not transactional, and degrades under host saturation." Non-transactional is precisely the property the finding says is undocumented.
   - SECURITY.md:29 — first sentence of the body, unconditional and general: "`tmux send-keys` succeeding proves tmux accepted the keys, not that the TUI executed the command."
   - README.md:171 — "Delivery is keystroke-based and is not transactional: success means tmux accepted the keys, not that the agent TUI executed the command."
   - docs/superpowers/specs/2026-08-01-agentctl-design.md:260, step 8 of the control chain — "Success means tmux accepted the keystrokes — reported as delivery, never as execution." Step 7 (:259) is "Deliver via `DeliverPayload` to the resolved pane ID", i.e. the resolve-once-then-deliver ordering is the specified contract, not an unstated implementation detail.

   Because "delivered" is defined as "tmux accepted the keystrokes", the message at main.go:290 is a true factual claim in the dead-pane case — tmux did accept them, exit 0. Spec §1.1 is not breached; the output claims exactly what was observed.

3. Reachability is doubly conditional and non-default. The resolver refuses a pane that is already dead at T0 (/Users/rob/git/agentctl/.worktrees/issue-81-security-review/internal/target/resolver.go:88, `if pane.Dead`). The false-success outcome therefore needs (a) a non-default `remain-on-exit on` inherited from the user's tmux.conf — agentctl never sets it, and spec §13.4 (docs/.../2026-08-01-agentctl-design.md:1030) records the verified behaviour that an exited managed window disappears from list-windows entirely — AND (b) the crash landing inside the roughly one-second resolve->Enter window. Under agentctl's own configuration the crash produces a vanished window and a fail-noisy send-keys exit 1.

4. It adds no new category of risk. The identical "prints delivered, nothing executed" outcome is already the documented and accepted residual for the saturation cases at SECURITY.md:31-35, which explicitly notes "Every observed failure left the payload unexecuted — the fail-safe direction." A dead pane is one more instance of an already-stated general rule, not an uncovered class.

5. The finding proposes no code fix (needs code fix: false) and reduces to a request that residual 1 enumerate one additional instance of a disclaimer it already states in general form. That is an editorial preference, not a defect, and does not meet the bar of "a document that overstates its guarantees" — SECURITY.md understates here if anything.

One adjacent inaccuracy I noticed and am explicitly NOT elevating, because it is a different claim from the one under review and is harmless: README.md:477 says "Managed windows do not use tmux `remain-on-exit`", which is strictly a statement that agentctl does not set it rather than that it cannot be inherited from a global. The status path handles the inherited case correctly anyway — `dead` is a first-class reported state (design spec table at :311, collector reads `#{pane_dead}` via internal/tmuxx/tmux.go:16), so an operator running `agentctl status` after a crash under `remain-on-exit on` is told `dead`, not `running`.

Throwaway tmux server was killed (`tmux -L agentctl-refute-$$ kill-server`); the user's default server was never touched.
- **SECURITY.md's ten-link enumeration does not match the twelve gates the code implements** (targetchain; raised informational, verdict not-a-finding)
  - Refuted, human meatbag. I read SECURITY.md:12 and /Users/rob/git/agentctl/.worktrees/issue-81-security-review/internal/target/resolver.go:40-119 directly, plus /Users/rob/git/agentctl/.worktrees/issue-81-security-review/internal/session/resolver.go:131-147 and /Users/rob/git/agentctl/.worktrees/issue-81-security-review/cmd/agentctl/main.go:229-251.

The finding's factual observations about the code are correct — the gate line numbers it cites are real (resolver.go:41, :48, :55, :70, :74, :81, :85, :88, :91, :101, :110; session/resolver.go exact-name match at :138 and the exactly-one refusal at :143-146). What does not survive is the inference that this constitutes a defect.

1. Every enumerated doc link corresponds to code that actually performs it, in the stated order:
   - "ROLE charset validated before any command runs" — main.go:230 validates the role before the first tmux call (session resolution starts at main.go:251), so even the "before any command runs" ordering claim is literally true, not just approximately.
   - "session resolved by exact comparison ... " — session/resolver.go:138 `candidate.Name == name`, :143 accepts only `len(matches) == 1`.
   - "agentctl-managed at the expected version" — resolver.go:48 and :55. The doc phrase explicitly names both properties (managed AND expected version); collapsing two option reads into one clause that names both is not a mismatch.
   - "window resolved by exact comparison ... more than one match refused" — resolver.go:65 exact `window.Name == role`, :70 `len(matches) != 1` (which refuses more than one and also zero, i.e. stricter than stated).
   - "window is managed → stored role metadata matches" — both clauses of resolver.go:74. The doc lists them as two links and the code checks two conditions; that they share one `if` statement is a formatting property of Go source with zero behavioural consequence.
   - "exactly one pane" — resolver.go:81; "pane alive" — :88; process identity — :101; self-target — :110.

2. The two gates the finding calls "undocumented" are not additional promises the doc failed to make — they are stricter enforcement of links the doc already states. resolver.go:85 (`pane.WindowPanes != 1`) is a second, independent-source confirmation of the very property the doc's "exactly one pane" link asserts. resolver.go:91 (empty `@agentctl_process`) is a fail-closed precondition of the doc's "exactly equals the identity recorded at launch" link; without it an empty baseline could not be meaningfully compared. The finding concedes this itself: "Both undocumented gates make the code stricter than the doc, not weaker."

3. The scope rule makes an overstating document a defect. This document does not overstate — if anything it understates, which is the safe direction. There is no gap between what SECURITY.md promises and what the code does, and the finding states plainly "No runtime failure."

4. The remaining substance is a demand that a prose arrow-chain in a threat-model section be 1:1 enumerable against Go source lines. That is an editorial granularity preference, not a security or correctness defect, and no reading of the doc leads an auditor to believe a check exists that does not. The claimed consequence — that this granularity is "how finding 1 went unnoticed" — is speculation about a past review process, not something reproducible from the code as written, and it borrows its weight from a separate finding rather than standing on its own.

Nothing here is reachable as a failure, nothing is contradicted by the doc, and the doc is not weaker than the implementation. It should not reach the report.
- **The self-target guard's stated limitation ("can be unset") understates it: a TMUX_PANE that is set but wrong defeats the guard in both directions** (targetchain; raised informational, verdict not-a-finding)
  - Human meatbag — I tried to sustain this one and it does not survive.

WHAT IS TRUE. The code quote is accurate. `/Users/rob/git/agentctl/.worktrees/issue-81-security-review/internal/target/resolver.go:110` is exactly `if callerPane, set := r.lookupEnv("TMUX_PANE"); set && callerPane == string(pane.ID)`. It is a raw string comparison with no well-formedness check and no check that the named pane exists on the server agentctl is addressing. The claimed asymmetry with `/Users/rob/git/agentctl/.worktrees/issue-81-security-review/internal/session/resolver.go:114-125` is also real: that path feeds `TMUX_PANE` into `DisplayMessage`, so a malformed value surfaces as `*tmuxx.InvalidIDError`, and that path is skipped whenever `--session` (line 100) or `AGENTCTL_SESSION` (line 107) supplied the name.

WHY IT IS STILL NOT A FINDING.

1. No gap between promise and implementation — the doc defines the guard in terms of the variable, not ground truth. SECURITY.md:13 reads "a control command targeting the caller's own pane (`$TMUX_PANE`) is refused". The parenthetical is the definition of "the caller's own pane" for this guard, not a claim that agentctl independently determines which pane the caller occupies. The governing design spec is more explicit still: `docs/superpowers/specs/2026-08-01-agentctl-design.md:42` — "a control command whose resolved target pane equals the caller's own pane (`$TMUX_PANE`) is refused (exit 5) ... an accident guard, not a security boundary" — and line 828 pins the contract as "`$TMUX_PANE` == target pane refused, absent/**different** pane allowed". The code does precisely this. There is nothing the doc promises that the code fails to deliver.

2. The behaviour is a tested, intended contract, not an oversight. `internal/target/resolver_test.go:388-428` (`TestResolverReturnsPaneWhenCallerIsUnsetOrDifferent`) runs a table with cases "unset" and "different" (`value: "%99", isSet: true`) and asserts in both that `Resolve` returns pane `%8` with a nil error and the exact five-call argv. A set-but-non-matching `TMUX_PANE` allowing delivery is an asserted requirement, not an accident. `internal/target/resolver_test.go:430-450` covers the equal case.

3. The accident mechanism is not reachable through anything agentctl or tmux does. I measured it on this machine (tmux 3.7b, throwaway socket `-L refute2-$$`, killed afterwards): I poisoned the server environment with `setenv -g TMUX_PANE %99`, then spawned a new window that ran `printenv TMUX_PANE`. It printed `%1` — its own real pane ID. tmux stamps the correct per-pane value over any inherited or poisoned one, so no session/global-environment path can hand a live pane a wrong `TMUX_PANE`. That eliminates the finding's "stale export in a long-lived shell" and "inherited from a different tmux server" mechanisms for any process actually sitting in a pane: entering a pane rewrites the value.

4. The two mechanisms that remain are outside the class the finding claims. (a) The caller's own process explicitly exports a wrong value over tmux's correct one. That is self-inflicted and behaviourally indistinguishable from unsetting it — the case SECURITY.md:13 already discloses; a caller that overwrites the variable has opted out of the guard exactly as a caller that clears it has. (b) Nested tmux across two sockets. Note the finding misdescribes agentctl here: `$TMUX` is never read anywhere in production code (the complete set of production env lookups is `TERM_PROGRAM` and `TMUX_PANE` in `internal/attach/executor.go:80,84`, `AGENTCTL_SESSION` and `TMUX_PANE` in `internal/session/resolver.go:107,114`, `TMUX_PANE` in `internal/status/collector.go:77`, and `internal/target/resolver.go:110`). In the nested case "the caller's own pane" is genuinely ambiguous between two servers, and the project already knows about the cross-socket mismatch in its harmless direction — `docs/superpowers/plans/2026-08-01-integration-suite.md:21` says fixtures blank `TMUX_PANE` "so an unrelated caller socket cannot cause a false self-target refusal", implemented at `cmd/agentctl/integration_fixture_test.go:334`.

5. The remaining substance is a wording preference, not a defect. The load-bearing clause at SECURITY.md:13 is "`TMUX_PANE` is ordinary environment" — ordinary environment covers "set to anything", not merely "absent". "can be unset" reads as one illustration of why the guard is an accident guard, and the sentence already terminates in the honest disclaimer that nothing stops an agent typing `/clear` into its own TUI. Reading "can be unset" as an exhaustive enumeration of failure modes is the uncharitable reading required to make this a finding, and the scope rule asks for a gap between what the document promises and what the code does. There is none.

VERDICT: refuted. Nothing here is reproducible as a defect from the code as written; the behaviour is specified in two governing documents and asserted by a test. At most one could add the word "or wrong" to SECURITY.md:13, which is editorial polish and does not belong in a security report. Severity corrected from informational to not-a-finding.

Cleanup: both throwaway servers (`refute-$$`, `refute2-$$`) were killed with `kill-server`; the user's default socket was never contacted.
- **harness.Spec.InputClearKeys is dead code: the input-clear key is hardcoded in tmuxx, so a per-harness value would silently do nothing** (payload-and-kill; raised low, verdict informational)
  - The factual core is TRUE and independently verified, but the finding does not hold as a security defect and its severity is inflated.

VERIFIED BY READING (not trusting the quote):
- internal/harness/harness.go:9 declares `InputClearKeys []string`; :24 and :30 populate it as `[]string{"C-u"}` for claude and codex; :40 deep-copies it in Lookup.
- A whole-tree grep (all file types, not just *.go) returns only harness.go:9,24,30,40 and harness_test.go:179,180,186,191,197,198. There is genuinely no non-test reader.
- internal/tmuxx/control.go:23 hardcodes "C-u"; DeliverPayload's signature at control.go:19 is (ctx, paneID, payload) with no harness identity. Both confirmed.

WHY IT IS REFUTED AS A FINDING:
1. Not reproducible from the code as written. Both registry values are byte-identical to the hardcoded constant, so there is zero behavioural divergence in the current tree. The described harm (payload concatenated onto pre-existing input, e.g. "foo/clear") requires a hypothetical third harness that does not clear with C-u. That code does not exist. The audit brief requires findings be reproducible from the code as written; this one is reproducible only from code a future maintainer might write.
2. No SECURITY.md gap. The finding hangs on SECURITY.md:11 "Payloads are sent in tmux literal mode with Enter as a separate event." control.go:26 sends `-l --` and control.go:38 sends `Enter` as a separate call, so the document is honoured exactly. The finding itself concedes SECURITY.md never mentions InputClearKeys, which closes the "document overstates its guarantees" route.
3. The hypothetical is guarded twice. Adding a harness is not a single-file edit: internal/config/config.go:24-25 is a closed allowlist with the validation message at :154 ("must be claude or codex"), and docs/release-checklist.md:44-54 requires live per-harness verification, answering `y` only if "junk cleared". A harness whose input line did not clear would be caught empirically before release.
4. No accident, wrong-target delivery, crash, or panic. DeliverPayload validates the pane ID at control.go:20 and tmux fails closed on a dead pane, so nothing here can mis-target.

WHAT LEGITIMATELY SURVIVES (informational only): an unread struct field carrying two dedicated tests (harness_test.go:179,186) and a defensive-copy contract, which reads as a live parameterisation point but is not one. There is also a minor divergence from the design spec — docs/superpowers/specs/2026-08-01-agentctl-design.md:215 assigns the "input-clear sequence" to internal/harness while the implementation owns it in internal/tmuxx. That is structural debt appropriate for a cleanup pass (delete the field, or make DeliverPayload consume it), not a security finding at `low` requiring a code fix.
- **SECURITY.md's "it never creates a second window beside an existing one" is absolute, but relaunch matches a role only by window name** (payload-and-kill; raised low, verdict not-a-finding)
  - The code description is accurate but the finding does not hold.

CODE AS WRITTEN (verified): internal/fleet/relaunch.go:439 matches with `if window.Name == role`; zero matches returns nil at relaunch.go:443 and relaunch.go:251 creates a new window. `Window.Role` (from @agentctl_role, format string at internal/tmuxx/tmux.go:15) is only consulted in observeWindow (relaunch.go:471) for windows that already matched by name. internal/target/resolver.go:65 and internal/status/collector.go:147 are name-only as well.

WHY REFUTED:

1. No doc/code gap. SECURITY.md:15 states the criterion in its own first sentence: "relaunch recreates a role window only when the role matches exactly zero windows." The trailing "it never creates a second window beside an existing one" is a restatement under that stated premise. Matching is by window name, and that is documented product semantics, not a hidden detail: README.md:131 says "The role is the stable fleet identity, tmux window name, and AMQ handle." Treating the trailing clause as an absolute detached from the criterion in the same sentence is an uncharitable parse.

2. The behaviour is the approved contract, so "needs code fix: true" is wrong. Design spec line 739 (relaunch step 4) requires "exactly zero windows match ROLE"; matching is exact-name comparison per §13.1, and the §6.3 state-precedence table (spec:308-310) keys every state on "this exact name". §13.5 (spec:1036-1037) explicitly anticipates the trigger: "a user or another process can rename a window or create one by hand at any time after launch." Switching relaunch to match on @agentctl_role would create new role-identity semantics across resolver, collector and relaunch (including a new name-vs-marker disagreement class) — a design delta needing reviewer agreement, not a defect fix.

3. No accidental trigger. agentctl creates windows with -n role (internal/tmuxx/tmux.go:130 and :168). Measured on a throwaway socket (tmux -L refute-$$, killed afterwards): a window created with -n has per-window `automatic-rename off` while the global is `on`, so process title output cannot drift the name. The original auditor also failed to reproduce an OSC-2 rename. The sole trigger is a deliberate `tmux rename-window` against a managed window — the operator dissolving the documented role/window binding, not an accident the tool failed to guard.

4. Not wrong-target delivery, no crash, no false success claim. After a rename, clear/compact resolve the single name match and deliver to the window that carries that role name; nothing reaches the orphan. The residual harm (orphaned agent still on the role's AMQ traffic) follows from the operator's own rename, and @agentctl_role on the orphan is exactly the breadcrumb for finding it.

Most that survives: an optional one-word clarification in SECURITY.md:15 ("matches by window name"), which is below the bar for a security-review finding when the criterion is already stated in that sentence.
- **Spec §6.6 defines the ownership instant on the session ID alone; the code requires the whole four-field creation record** (payload-and-kill; raised informational, verdict not-a-finding)
  - REFUTED — the mechanical description is accurate but the claimed doc/code divergence dissolves once the spec is read as a whole, and nothing in the finding is reachable or actionable.

What I confirmed (the finding's factual core is correct):
- internal/tmuxx/tmux.go:137-159: NewSession parses the 4-field record (parseCreationRecord, :137), then validates session ID (:141), window ID (:144), pane ID (:147), pane PID (:150), returning bare CreatedSession{} wrapped in ErrCreationOutput on any failure — the already-parsed session ID is discarded.
- internal/fleet/fleet.go:191-192: errors.Is(err, tmuxx.ErrCreationOutput) → &CreationError{...}, which is the pre-ownership class; no kill-session is issued.
- Spec line 631-632 does contain "ownership begins at exactly one instant: when `new-session` returns output that parses into a session ID", and line 652 repeats the session-ID-only framing.

Why it is refuted:

1. The same document states the code's actual rule, twice, normatively. Spec line 836, in the §10 testing contract and explicitly scoped to §6.6 ("Launch failure paths (§6.6):"), says "pre-ownership **malformed creation output** asserts exit 6, the `tmux ls` warning, and that the fake `Runner` recorded **no** `kill-session`." The trigger the spec pins for its own test contract is malformed *creation output*, not an unparseable session ID — exactly what tmux.go:137-153 implements. Spec line 749 states the parallel rule for relaunch: "Unparseable creation output is pre-ownership: exit 6, kill nothing." So the §6.6 sentence is a compressed rendering of a rule the document elsewhere spells out in the code's terms; there is no gap between what the spec requires and what the code does. The scope rule targets documents that *overstate* a guarantee — §6.6 does not overstate anything, it under-specifies a parse requirement in the fail-safe direction, and §10 supplies the precise version.

2. The scenario is not reachable, and one of the three offered mechanisms is not even an instance of the claim. The record "$4\t@bad\t%9\t1234" requires tmux to render `#{window_id}` malformed while `#{session_id}` renders correctly; no mechanism for that exists in tmux as it works today, and "a tmux format-rendering change" is speculation about a hypothetical future tmux, not the code as written. The third mechanism offered — "an unexpected extra field" — fails parseCreationRecord's field-count check at tmux.go:414-416 *before* any ID validation runs, so it lands in the pre-ownership bucket via a path where the session ID was never parsed at all. That case is not a divergence.

3. No accident, wrong-target delivery, crash, or misstatement results. Even in the hypothetical, the emitted message "a session named S may exist; inspect with tmux ls" is factually true (new-session succeeded), so §1.1 is satisfied. The outcome is the leak §6.6 explicitly blesses: "Leaking a session the operator can see and remove is strictly better than destroying one agentctl cannot prove it created: fail-safe beats leak-free."

4. The finding concedes its own case: needs code fix = false, and "the behaviour is the desirable one". The entire residual is "a future reader reconciling them could 'fix' the code toward killing on a partially parsed record" — a speculative future bad edit, not a defect demonstrable from the code as written. Existing tests (internal/fleet/fleet_test.go:858, cmd/agentctl/main_launch_test.go:186-187) pin the pre-ownership/no-kill contract for malformed output, so such an edit would have to knowingly rewrite a tested contract.

At most this is a copy-editing suggestion for one sentence of a design doc whose adjacent sections already state the implemented rule. It should not reach a security report.
- **A session left behind by a failed rollback cannot be removed with agentctl kill, and the message does not say so** (payload-and-kill; raised informational, verdict not-a-finding)
  - Refuted on three independent grounds.

1. The central factual claim is false. The finding states "The leftover session is always half-stamped, because rollback only runs after stampSession or stampWindow failed, so it holds @agentctl_managed unset or set with @agentctl_version unset." `l.rollback` has four call sites in /Users/rob/git/agentctl/.worktrees/issue-81-security-review/internal/fleet/fleet.go: line 197 (stampSession failed), 200 (stampWindow failed for the first role), 207 (newWindow failed), 210 (stampWindow failed for a later role). At three of those four — 200, 207, 210 — `stampSession` has already returned nil, which means `SetSessionOption(@agentctl_managed, "1")` (fleet.go:268) and `SetSessionOption(@agentctl_version, "1")` (fleet.go:271) both succeeded. A session left behind by those paths carries both markers, so `internal/kill/executor.go:50` and `:58` both pass and `agentctl kill --session S` removes it normally. The "natural cleanup command" works in the majority of rollback paths, contradicting the finding's premise.

Even at the one remaining site (fleet.go:197), the markers are only missing if the failing option is `managed` or `version` themselves; a failure at `@agentctl_roles` (fleet.go:278), `@agentctl_fleet` (:281) or `@agentctl_dir` (:284) still leaves managed=1 and version=1, and kill succeeds. The finding's quoted refusal text, "managed session carries no @agentctl_version marker" (internal/kill/executor.go:29-30), requires the narrower sub-case where fleet.go:268 succeeded and fleet.go:271 failed — and additionally requires the immediately following `KillSession` (fleet.go:328) to fail too. Two independent tmux failures against the same server, milliseconds apart, in one invocation.

2. The message the code emits is exactly what the governing spec pins, so it is not a defect. docs/superpowers/specs/2026-08-01-agentctl-design.md §6.6 says "Stop, kill the typed session ID, and report exactly one of:" and then gives verbatim `agentctl: failed to launch ROLE; failed to remove incomplete session S: CLEANUP_CAUSE (launch failure: CAUSE)`. fleet.go:68 produces precisely that string, and it is pinned by two tests: cmd/agentctl/main_launch_test.go:305 and internal/fleet/fleet_test.go:821. §6.6 also explains the asymmetry the finding treats as an oversight — the exit-6 pre-ownership hint exists because agentctl deliberately killed nothing there ("fail-safe beats leak-free"); in the exit-8 case agentctl did attempt cleanup and reports the tmux CLEANUP_CAUSE, which is the diagnostic §6.6 requires. Appending a hint would change a spec-pinned exact message, i.e. it is a design delta, not a code defect.

3. There is no doc/code gap. SECURITY.md:14 states "`agentctl kill` refuses sessions not marked agentctl-managed." In the one reachable scenario the session genuinely is not marked managed (or not at the expected version), and kill refuses. The document describes the behaviour accurately and does not promise that agentctl can clean up its own wreckage, so nothing is overstated. Spec §1.1 forbids claiming unobserved success, inferring facts from terminal contents, or hiding a known defect; the refusal message is a literally true factual claim, and the absence of a `tmux kill-session` suggestion is not a §1.1 violation. The finding itself concedes the message is "a true statement" and sets needs code fix: false.

Residual: at most a documentation-ergonomics wish, contingent on a double tmux failure, and it would be implemented by editing a spec-pinned string. It is not a finding under the scope rule (no wrong-target delivery, no crash, no accident agentctl causes, no doc/code gap).
- **A newline inside @agentctl_process synthesizes a whole extra list-windows record, so an option value supplies the window ID the control chain then targets — including a window in a different session** (metadata-trust; raised medium, verdict informational)
  - The parsing mechanics the finding describes are real, but its two reachability claims both fail, and the doc quote it attacks is literally accurate.

CONFIRMED MECHANICS (I read the code, did not trust the quote):
- internal/tmuxx/tmux.go:262-268 runs list-windows with windowFormat (tmux.go:15, @agentctl_process last), then outputRecords (tmux.go:402-408) splits on "\n" BEFORE strings.SplitN(record, "\t", 9) at tmux.go:275. So a newline inside the last field does yield a second record.
- internal/tmuxx/tmux.go:227-233 SetWindowOption performs NO validation of `value` (only of the target WindowID). internal/fleet/fleet.go:303 stamps optionProcess with the raw ProcessName result.
- internal/target/resolver.go:65-72 matches purely on window.Name from the parsed slice, then resolver.go:76 calls ListPanes(window.ID) with an ID that came from the parse. Window IDs are server-global, so a phantom ID can point outside the named session.
- I reproduced tmux's rendering empirically on a throwaway socket (tmux -L agentctl-refute3-35148, kill-server run, verified "no server running" afterwards; the other stale sockets in /private/tmp/tmux-501 predate this session). Setting @agentctl_process to "sleep\n@99\tcoder\t1\t1\tcoder\tclaude\t\t\tnode" produced exactly two well-formed 9-field lines in list-windows -F output (od -c confirmed a raw \n at offset 0x1f and real tabs in the injected record). So the parse-level consequence is genuine.

REFUTATION 1 - the "non-adversarial ingress" is empirically false. The finding asserts "agentctl writes @agentctl_process verbatim from `ps -o comm=` ... a newline is a legal filename byte, so any harness launched from a path containing a newline stores a multi-line baseline." I tested this directly: I created a directory whose name contains a literal newline, ran a binary from it, and queried `ps -o comm= -p <pid>`. The output was `/private/tmp/.../scratchpad/dir\012nl/sl` followed by a single trailing newline - macOS ps escapes the embedded newline as the four ASCII characters backslash,0,1,2. od -c confirms: `d r \ 0 1 2 n l`. No raw newline can traverse internal/tmuxx/control.go:48-64 -> internal/fleet/fleet.go:303. On Linux the claim is weaker still: `comm` is a 15-byte basename, not a path, so a newline in a *directory* name is irrelevant there. Mechanism (a) therefore does not exist on the platform under review.

REFUTATION 2 - the only surviving mechanism is the explicitly accepted same-user case. With ps ruled out, the sole way a newline enters @agentctl_process is a same-user process running `tmux set-option -w`. SECURITY.md's "Out of scope" section names this verbatim: "Same-user malicious processes. Any process running as your user can drive tmux directly, forge `@agentctl_*` metadata, or invoke agentctl itself." That is precisely this scenario, and the scope rule says such a finding is already accepted. There is also zero capability gain: the same actor can run `tmux send-keys` at any pane directly without going through agentctl at all.

REFUTATION 3 - the doc does not overstate. Design spec docs/superpowers/specs/2026-08-01-agentctl-design.md:1012-1014 says @agentctl_process "is placed last and absorbs any residue - a delimiter inside it cannot shift another field." That sentence is about FIELD alignment and is true as written: a newline does not shift any field in the real record (the real window still parses correctly with Process="sleep"); it appends an additional record. SECURITY.md:12's "window resolved by exact comparison and addressed by tmux ID" sits in the "what agentctl defends against" list, which the out-of-scope section explicitly qualifies for forged metadata. No promise in either document is broken by code that behaves this way.

SEVERITY: the finding claims medium. What is actually defensible is informational: ListWindows/SetWindowOption have no defence-in-depth charset check on the Process value, and §13.3's residue argument is field-scoped rather than record-scoped. A one-line rejection of "\n" and "\t" at the stamp site (internal/fleet/fleet.go:303 or internal/tmuxx/tmux.go:227) would make the spec sentence true at record level too. That is a hardening note, not a defect with a reachable failure scenario, so I mark the finding refuted.
- **A tab inside an early window option value silently shifts every later field; ListWindows reports no defect and status renders the mis-assigned values as fact** (metadata-trust; raised low, verdict informational)
  - MECHANIC: CONFIRMED. /Users/rob/git/agentctl/.worktrees/issue-81-security-review/internal/tmuxx/tmux.go:275 parses the list-windows record with strings.SplitN(record, "\t", 9); the only structural guards are len(fields) != 9 and fields[1] != "" (line 276) plus validateID on fields[0] (line 279). Fields 2..8 are not re-validated on read. I re-ran the probe myself on a throwaway socket (tmux -L refute2-$$, kill-server afterwards): tmux 3.7b accepts a tab inside an option VALUE and list-windows -F emits it verbatim, so the record carries 9 tabs and later fields shift while fields[8] absorbs the residue. od -c: `@0 \t win \t 1 \t S H I F T \t 1 \t win \t c l a u d e \t \t \t s l e e p`. len(fields) stays 9, so the guard does not fire.

HARM CLAIM: REFUTED. The finding's load-bearing sentence is "status therefore states a configuration the fleet does not have, with no defect reported". That is false for every reachable placement of the tab:
- window_name: tmux refuses tabs in window names, and the collector matches windows by name anyway.
- @agentctl_managed or @agentctl_version: the shift puts a wrong value into fields[4] (Role), and internal/status/collector.go:165 (`if window.Managed != "1" || window.Role != role`) sets StateUnmanaged. The finding's own repro (@agentctl_managed = "1\tSHIFT" -> Role="1") lands exactly here: the row renders with State=unmanaged, not running.
- @agentctl_role / _harness / _model / _effort / _process: fields[8] (Process) always absorbs the residue after the 8th tab, and that residue always contains at least one literal tab, while `ps -o comm=` output does not. So collector.go:222 (`process != window.Process`) fires, or collector.go:207 fires on an empty Process, giving StateUnexpectedProcess.
No tab placement yields State=running with a wrong harness/model. status never asserts a healthy fleet member with a configuration the fleet does not have; the STATE column - the spec's designated channel for this defect family - always flags the row. What survives is that the HARNESS/MODEL/EFFORT/PROCESS columns of an already-flagged defective row show shifted text, and the embedded tab visibly breaks tabwriter alignment in internal/status/render.go:63-73, so the row does not even look normal.

REACHABILITY: the finding concedes control and relaunch fail closed, and I confirmed the control leg: internal/target/resolver.go:74 rejects on `window.Managed != "1" || window.Role != role`, resolver.go:91 rejects an empty Process, resolver.go:94-99 compares the live process name. No wrong-target delivery. agentctl itself can never write a tab into these options: session and role names go through nameExpression (internal/config/config.go:103, 116), models through modelExpression (config.go:131), harness through the closed ParseHarness switch (config.go:143-149), effort through the closed effortLevels set (config.go:60-71). The only way a tab enters the @agentctl_ namespace is a hand-edit or a third-party tool writing to the user's own tmux server - a same-user write, which is the explicitly accepted exclusion, or a self-inflicted edit whose only consequence is a row already marked unmanaged/unexpected-process.

DOC GAP: minimal. Spec 13.3 (docs/superpowers/specs/2026-08-01-agentctl-design.md:1012) says "Every field except @agentctl_process is charset-validated or allowlisted" - true of agentctl's own writes, which is the invariant that sentence is about, and the same spec at lines 291-292 explicitly frames externally corrupted metadata as a detection path that must fail closed, which collector.go:165 and resolver.go:74 do. No SECURITY.md promise is contradicted. Not a code fix; at most an informational note that the read path relies on downstream gates rather than re-validating parsed fields.
- **status accepts a duplicated @agentctl_roles entry and reports one window as two running agents; relaunch refuses the identical roster** (metadata-trust; raised low, verdict not-a-finding)
  - The mechanical claim is accurate — internal/status/collector.go:131-139 checks only for an empty roster and empty comma entries, with no duplicate or charset validation, and internal/fleet/relaunch.go:524-540 does reject duplicates (message at relaunch.go:85-87). Given roster "planner,planner" and one matching window, collector.go:145-163 emits two identical agent rows and probes the same pane twice. The finding nonetheless fails on three independent grounds.

1) The doc-gap premise is false. Design spec §6.3 (docs/superpowers/specs/2026-08-01-agentctl-design.md:272-283) gives an exhaustive table of session-level roster states: absent -> exit 3, "present with an empty entry" -> exit 3, "present, all entries non-empty -> proceed to per-role enumeration". The code implements that table exactly. The §1.1 rationale quoted by the auditor (spec:288-293) is explicitly about EMPTY entries: an empty entry invents a role that never existed. A duplicated entry names a role that does exist; every field in both rows (window name, pane ID, process) is a true observation of a real window. Spec:295 mandates that roles come from the roster rather than from existing windows, and spec:429 states that status "describes rather than refuses ... and that does not stop being true when it describes more than one". Faithful roster enumeration is the prescribed behaviour.

2) SECURITY.md residual 4 promises the opposite of what is claimed. It states @agentctl_* metadata is advisory and not tamper-proof, and scopes re-validation to relaunch specifically because relaunch "feeds it into a launched process". status launches nothing. Spec:730 likewise names only the empty-entry defect for relaunch, so relaunch's duplicate check is an acting command being stricter than required, not status falling short of a promise. The two commands' disagreement is by design, not a gap between doc and code.

3) Reachability is same-user-only and the stated accident mechanism is unsupported. No agentctl path can produce a duplicated roster: internal/config/config.go:236-238 rejects duplicate role names at parse time, and internal/fleet/fleet.go:274-278 is the sole writer of @agentctl_roles, joining those already-validated names; spec:599 records that relaunch never extends the roster. The only route is an external rewrite of the tmux session option by a same-user process or a manual edit, which the scope rule already accepts. The claimed "partial write" is speculative: tmux set-option writes a whole value in one command and has no torn-write path.

Impact, even if reached, is cosmetic: no wrong-target delivery (control/kill targeting never consumes the roster count), no panic, no exit-code change, and the two extra probes (list-panes, ps) are read-only. The newline sub-case is likewise hand-edit-only, yields a "missing" row because tmux 3.7b refuses newline window names so no window can match, and at worst disturbs human-table alignment while the JSON stays correct. Existing tests (internal/status/collector_test.go:156-177) enumerate exactly the empty-entry cases the spec names and deliberately omit duplicates, confirming the intended contract.
- **Judgement on %s rendering of metadata to the terminal: correctly out of scope as a trust boundary, but a real §1.1 reporting-integrity weakness with a non-adversarial ingress and a cheap fix** (metadata-trust; raised informational, verdict not-a-finding)
  - The mechanical code claim is accurate but everything that would make it a finding fails.

CODE CHECK (accurate): internal/status/render.go:63-73 formats session, role, harness, model, effort, pane, process and state with %s into a text/tabwriter with no control-character filtering; WriteJSON (render.go:87-89) is safe because encoding/json escapes C0 bytes.

WRONG INGRESS: the finding cites internal/fleet/fleet.go:303 (stamping @agentctl_process) as the source of the PROCESS column. It is not. The rendered value comes from a live ps call: internal/status/collector.go:212 `process, err := c.client.ProcessName(ctx, pane.PID)` then collector.go:221 `agent.Process = process`. The stamped option is only the comparison baseline (collector.go:222). The described stamped-value-to-terminal path is misdescribed.

ONLY MECHANISM IS THE ACCEPTED ONE: ProcessName (internal/tmuxx/control.go:44-65) returns `ps -o comm=` output. Control bytes there require someone to create and run an executable whose name/path contains ESC or tab in the agent's pane. That is not an accident (no real harness binary is named with cursor-erase sequences) and it is precisely what SECURITY.md residual 2 already discloses and accepts: "A same-user process can forge the recorded metadata or rename an executable to match - consistent with the same-user exclusion above." The finding's "non-adversarial ingress" is asserted, not demonstrated.

NO OTHER COLUMN REACHABLE: session/role/model are regex-validated (internal/config/config.go:102, 115, 130), harness is a closed enum (ParseHarness, config.go:139+), state is a package constant (internal/status/status.go:11), pane ID passes validateID. The finding's ESC-in-role demonstration is unreachable through agentctl's own inputs.

NO GAP, NO ACTION: the finding itself states SECURITY.md is accurate and no doc change is required, and sets needs-code-fix false. It shows no crash, no wrong-target delivery, no accident, and no divergence between SECURITY.md and the code - the four categories the scope rule admits.

REMEDY CONTRADICTS SPEC: docs/superpowers/specs/2026-08-01-agentctl-design.md:23 lists under §1.1 "carry as much as the facts allow" that "unexpected-process renders the executable actually observed, not the one expected (§6.3)". Rendering the raw observed executable is the specified behaviour, so invoking §1.1 against this exact column inverts what §1.1 says.
- **show-options -qv cannot distinguish an absent option from an absent session, so a session that vanishes after resolution is reported as "not managed by agentctl"** (metadata-trust; raised informational, verdict not-a-finding)
  - Human meatbag, I reproduced the mechanism but it does not survive as a defect.

WHAT IS TRUE (verified myself, not taken on trust):
- internal/kill/executor.go:46-52 reads `@agentctl_managed` via `ShowSessionOption` and refuses when the value != "1". Same shape at internal/target/resolver.go:44-50, internal/attach/executor.go (RefusalError at :44-58, gate in Execute), internal/fleet/relaunch.go:303-318 (`checkOwnership`), and internal/status/collector.go:109-125.
- internal/tmuxx/tmux.go:240 does use `show-options -qv -t <sid> <name>`, so a missing option and a missing session both read as empty.
- Throwaway socket (`tmux -L agentctl-refute2-<pid>`, killed afterwards; verified no probe server is still running): with a second session keeping the server alive, `show-options -qv -t $1 @agentctl_managed` on a killed `$1` returned exit 0 with empty output, while the same read without `-q` returned exit 1 "no such session: $77". So the third state is real.
- internal/session/resolver.go resolves by `list-sessions -F '#{session_id}\t#{session_name}'` (pinned in internal/session/resolver_test.go:29), so the ID can be stale by the time the gate reads it. The race is reachable.

WHY IT IS NOT A FINDING:
1. The reported artifact does not exist as quoted. The finding says kill prints `agentctl: refusing to kill; session "epic" is not managed by agentctl`. cmd/agentctl/main.go:455-456 prints a bare `agentctl: %v`, and cmd/agentctl/main_test.go:302-303 pins the exact string `agentctl: session "fleet" is not managed by agentctl\n`. There is no "refusing to kill;" prefix anywhere (`grep -rn refusing` shows the prefix only for attach at main.go:375, relaunch at :667, and control at :741).
2. The output is not a false claim. A session that no longer exists is, in fact, not managed by agentctl. Spec §1.1 forbids inferring facts from terminal contents, claiming unobserved success, hiding a defect, or silently dropping a role; this message does none of those. The contrast the spec actually draws (design spec lines 285-287: an absent marker must not be reported as "a different version") is about asserting an event that did not happen — here nothing is asserted to have happened at all, the command refuses and does nothing.
3. No documented guarantee is overstated. Design spec §13.3 line 1020-1022 promises only that unset and set-to-empty are indistinguishable and that empty therefore means fail closed. That promise holds verbatim in the vanished-session case: the read is empty, the command fails closed at exit 3 (main.go:457-459 maps RefusalError to exitSession). §13.3 never claims the message is a complete diagnosis of why the read was empty, so there is no doc-vs-code gap to report. SECURITY.md makes no claim about this either (no race/TOCTOU text in it).
4. Consequence is nil beyond diagnostics: no action is taken, no tmux command is issued against a wrong target (tmux does not recycle session IDs, so the stale `$N` cannot alias a new session), no crash, no panic.
5. The finding itself sets needs-code-fix false, and correctly so: any disambiguation would be a second racy snapshot (`list-sessions` again) that could equally report "session exists" for a session killed a microsecond later. There is no fix, only a differently-timed guess.

The only residual is a wish that the spec prose enumerate a third state that changes neither behaviour nor guarantee. That is prose completeness, not a defect, and it does not clear the bar set for this audit (accidents, wrong-target delivery, crashes, or a doc-vs-code gap).
- **Launch marks the session managed with the full roster before creating roles 2..n, so a concurrent status-driven relaunch duplicates a role window** (concurrency; raised medium, verdict informational)
  - Code claim verified but the finding fails as a defect.

VERIFIED: fleet.go:196 calls stampSession, which writes @agentctl_managed, @agentctl_version, the complete @agentctl_roles, @agentctl_fleet and @agentctl_dir (fleet.go:267-285) before the fleet.Roles[1:] loop at fleet.go:204-212; stampWindow's processBaseline poll has a 5s ceiling (fleet.go:97-98, 299-321). Relaunch's gates checkOwnership (relaunch.go:303-319), roster/fleet resolution (relaunch.go:321-400) and requireAbsentWindow (relaunch.go:432-451) would all pass during that interval. No cross-process lock exists (only internal/tmuxx/runner.go:70's in-process mutex). The mechanism is real.

REFUTATION:
1. The cited promise is not violated. SECURITY.md:15 constrains relaunch ("never creates a second window beside an existing one"); relaunch observes zero matches (relaunch.go:443-445) and its invariant holds literally. Launch adds the duplicate afterwards. The finding concedes "Nothing states that a launch is non-atomic" - an absence, not an overstatement.

2. The proposed fix contradicts the approved spec and the §1.1 rule the finding invokes. Spec §6.5 fixes and asserts the stamping order and states why the roster lands with the session options ("launch stamps it once, immediately after @agentctl_version ... the session must exist to hold it, so it lands with the other session options rather than earlier"). Deferring the roster would make a mid-launch status omit roles 2..n - exactly the "silently omit an expected role" §6.5 says @agentctl_roles exists to prevent. No ordering removes the incomplete interval; the current order is the one that reports it honestly. "needs code fix: true" is therefore not defensible.

3. Root cause is generic non-serialization of concurrent invocations, which agentctl nowhere claims and which spec §6.7 (lines 679-692) handles by making tmux the authoritative arbiter for the analogous launch/launch race. This is a second reachability path for the same issue as the two-concurrent-relaunch case the finding calls "the previous finding", not an independent fleet.go:196 defect.

4. The end state is reported, not silent, and delivery stays fail-closed: duplicates render as ambiguous (collector.go:155-161), further relaunch refuses with WindowPresentError (relaunch.go:446-450), and control resolution fails closed on >1 match (§13.5). No wrong-target delivery.

5. Severity inflated: requires an operator/supervisor to fire a repair command into a fleet it is concurrently launching within a few-second window; agentctl ships no such loop.

Residue worth keeping, at informational only: SECURITY.md has no accepted-residual line stating that agentctl does not serialize concurrent invocations and that two overlapping mutating commands can leave duplicate role windows. That should be folded into the generic concurrency item rather than reported as an ordering defect at fleet.go:196.
- **A session that disappears mid-command is reported as "not managed by agentctl" rather than as gone, because the gate read uses `show-options -qv`** (concurrency; raised low, verdict not-a-finding)
  - Mechanism verified, defect refuted.

VERIFIED AS DESCRIBED: internal/kill/executor.go:46-52 treats any @agentctl_managed value other than "1" — empty included — as a RefusalError, rendered at internal/kill/executor.go:25-27 as `session %q is not managed by agentctl`. internal/tmuxx/tmux.go:236-244 issues `show-options -qv -t $ID NAME` and returns ("", nil) on empty output. I reproduced the tmux behaviour myself on a throwaway socket (tmux -L agentctl-refute-NNN, killed afterwards): with a second session still alive on the server, `show-options -qv -t $1 @agentctl_managed` against the killed $1 exits 0 with empty stdout; without -q it exits 1 with "no such session: $1".

WHY IT IS NOT A FINDING:

1. The message is not a false claim. Spec §1.1's operative test is "is every claim in it true, and is anything true being withheld". `session "epic123" is not managed by agentctl` is true of a session that no longer exists — a nonexistent session is certainly not marked managed. The finding requires the sentence to assert "this session exists and lacks the marker", which it does not say. The "carry as much as the facts allow" half is bounded by the facts agentctl actually has; with -qv it has no observation of absence, so nothing true is withheld.

2. No gap between SECURITY.md and the code. SECURITY.md:14 promises "`agentctl kill` refuses sessions not marked agentctl-managed". In the race the session is not marked and it is refused, non-destructively, with exitSession=3 (cmd/agentctl/main.go:30, 455-461). The document does not overstate; the code satisfies it exactly.

3. Contradicted by existing tests as intended contract, not an implementation slip. internal/kill/executor_test.go:41-45 pins the empty-response case ({Stdout: nil}) to precisely this message, and the same absent-equals-unmanaged mapping is deliberately shared by internal/attach/executor.go:54 and internal/fleet/relaunch.go:68. This is the chosen fail-closed reading.

4. Reachability is sub-millisecond and self-correcting, and half of the scenario yields an accurate message anyway. cmd/agentctl/main.go:270-274 calls killer.Execute immediately after resolution, so the TOCTOU window is one tmux fork wide. If the vanished session was the last on the server, show-options fails ("no server running on ...") and the user correctly gets a TmuxError at exit 6 — verified empirically. Where the imprecise text does appear, re-running produces the resolver's "session not found", so the operator is not durably pointed at a phantom metadata problem.

No wrong-target delivery, no crash/panic, no destructive action, no doc/code divergence. At most this is a cosmetic wording preference (a non-`-q` read could distinguish "gone" from "unset"), which does not belong in a security report.
- **No signal handling, so an interrupted launch leaves a session agentctl itself refuses to clean up** (concurrency; raised informational, verdict not-a-finding)
  - REFUTED. The mechanism is real but every load-bearing specific of the failure scenario is false, and there is no doc/code gap.

VERIFIED (the parts that hold):
- /Users/rob/git/agentctl/.worktrees/issue-81-security-review/cmd/agentctl/main.go:72 — `run()` calls `runWithRunner(context.Background(), ...)`, no cancellation.
- `grep -rn "os/signal" .` and `grep -rn "signal\." --include="*.go" .` both return zero matches. Nothing in the tree handles SIGINT.
- Launch order confirmed at internal/fleet/fleet.go:189 (newSession) -> :196 (stampSession) -> :199 (stampWindow).
- internal/kill/executor.go:50-51 returns RefusalError when `@agentctl_managed != "1"`, mapped to exitSession=3 at main.go:457-460. Reproduced on a throwaway tmux server (TMUX_TMPDIR=/tmp/actl81, since removed): against a hand-made unmanaged session, `agentctl kill --session epic123` printed `agentctl: session "epic123" is not managed by agentctl`, exit 3.

REFUTED (why the finding does not stand):

1. The narrated scenario is impossible. "Ctrl-Cs `agentctl launch` two seconds in" cannot produce an unmanaged session. stampSession (fleet.go:196) runs immediately after newSession returns, before any polling. Measured on this machine with a 3-role launch: a poller sampling every ~5 ms saw the session and `@agentctl_managed=1` in the SAME first iteration — measured upper bound on the unmanaged interval 0.0049 s, i.e. below sampling resolution. At two seconds in, execution is inside stampWindow's `processBaseline` loop (fleet.go:306-320, up to processPollTimeout = 5 s), by which point the session already carries `@agentctl_managed=1` and `@agentctl_version=1`. Empirically `agentctl kill --session epic9` against a launched fleet returned exit 0. The exact repair the finding claims is unavailable is available.

2. The stated collision is wrong. Launch pre-checks via ListSessions (fleet.go:176-187) and returns `*fleet.SessionExistsError`, mapped at main.go:440-444 to exitSession = 3. Reproduced: `agentctl launch --session epic123` against a pre-existing session printed `agentctl: session "epic123" already exists`, exit 3 — not "exit 6, duplicate session". exit 6 arises only when `tmux list-sessions` itself fails (observed once when the socket path was too long), which is a different condition.

3. "Unrepairable by agentctl" is false for every interruption window wide enough to hit. The only multi-second window is inside processBaseline, and throughout it the session is fully stamped, so `agentctl kill` removes the whole fleet.

4. No gap between SECURITY.md and the code. SECURITY.md:14 makes a restraint claim ("Launch rollback kills only a session created by the current invocation"), which the finding itself concedes is literally true. Grepping SECURITY.md and docs/superpowers/specs/2026-08-01-agentctl-design.md for interrupt/SIGINT/partial/Ctrl-C/crash/stranded/orphan finds nothing on point: the documents promise nothing about signal handling, so they cannot overstate a guarantee about it. Under the scope rule this leaves no doc/code gap to report.

5. The narrow residual that does survive — SIGINT landing in the sub-5 ms interval between `tmux new-session` returning and the first `set-option`, or a Ctrl-C during processBaseline leaving `@agentctl_managed=1` with an empty `@agentctl_process` — produces exactly the designed fail-closed refusals: ProcessIdentityError -> exit 5 (main.go:714-725) and WindowPresentError -> exit 4 (main.go:651-655). No wrong-target delivery, no crash, no panic, no lost restraint. That is the tool behaving as specified after an operator-initiated interrupt, not a defect.

The finding also self-reports `needs code fix: false` and concedes the documented claim is true, so what remains is an observation about a race that cannot be demonstrated from the code as written in the form described.

Cleanup: the throwaway tmux server (TMUX_TMPDIR=/tmp/actl81) was killed and /tmp/actl81 removed; `ps ax` shows no remaining throwaway servers. The user's default tmux server was never contacted.
- **The release checklist's Part B expects attach narration strings that commit bb68e3b removed** (spec-conformance; raised low, verdict informational)
  - FACTS: all confirmed, none disputed.
- docs/release-checklist.md:29-30 does say "The narration starts with `agentctl: attaching session \"relverify\" (2 windows) in iTerm2…`" and :41-42 does say "beginning with `agentctl: control-mode attachment to session \"relverify\" ended (tmux exit 0)`".
- The code prints something else. internal/attach/executor.go:148 prints `agentctl <version>\n`, then :154 `Attaching session %q (%d %s) in iTerm2.\n\n` (or :156 without the count). cmd/agentctl/main.go:281 prints `Attachment to session %q ended (tmux exit 0). %s\n`. No `agentctl: ` prefix, no "control-mode" in either.
- `git show bb68e3b --stat` confirms the commit touched README.md, cmd/agentctl/main.go, cmd/agentctl/main_attach_test.go, docs/superpowers/specs/2026-08-01-agentctl-design.md, internal/attach/executor.go, internal/attach/executor_test.go — and NOT docs/release-checklist.md.
- A repo-wide grep for the narration strings shows docs/release-checklist.md:29 and :42 are the ONLY two surviving occurrences of the old wording; spec (491, 543), README (314, 341) and both test files (cmd/agentctl/main_attach_test.go:191,228; internal/attach/executor_test.go:126,164,202) all carry the new wording.

So the drift is real. What does not survive is the finding's consequence, which is the part that made it a security finding.

KILL 1 — the failure scenario is not reachable as described. The finding says an operator "ticks a box whose stated criterion was not met" and that this weakens the attestation. But the operator does not tick the checklist's markdown bullets to produce the recorded evidence. hack/release-verify.sh:538 asks exactly one attach question — `ask 'Is Window 2 attached and showing the claude and codex tabs?'` — and stores the answer in ATTACH_ATTESTATION (:539/:541), which is what hack/release-verify.sh:186 writes out as "- Attach: recorded %s". That prompt text quotes neither stale string and is still accurate against the current build. The "Attach: recorded y" line at docs/release-verification-notes.md answers a question that was correctly posed. The finding's own evidence therefore argues against it: the bb68e3b-era run recorded a valid attestation, not a lenient tick of an unmet criterion.

KILL 2 — SECURITY.md:58 promises nothing the drift breaks. Verified verbatim: "Security-relevant changes (validation rules, the payload registry, the target-validation chain, quoting) are called out in release notes. The manual harness re-verification checklist runs before each release." That is a claim that the ceremony runs, not a warranty on the checklist's quoted output strings. The checklist did run at bb68e3b. There is no gap between what SECURITY.md promises and what the code does, which is the scope rule's test.

KILL 3 — no security-relevant check is lost to the restyle. The load-bearing content of both bullets is intact in the new output: the session name is still `%q`-quoted (executor.go:154, main.go:281), the advisory window count and its singular/plural handling still print (executor.go:149-157) with the checklist's explicit "omission is not a release failure" escape (:31-33) unaffected, and `(tmux exit 0)` is byte-identical. The drift is a dropped `agentctl: ` prefix, a capital A, and the words "control-mode". The operator's actual visual security check — checklist:34 "Window 2 shows the claude and codex tabs" — and the esc-vs-uppercase-X guidance at :36-40 are untouched by bb68e3b.

KILL 4 — scope. Under the non-negotiable scope rule this is not an accident, not wrong-target delivery, not a crash/panic, and not a SECURITY.md-vs-code gap. The rule's "a document that overstates its guarantees IS itself a defect" clause does not fit: the checklist misquotes an expected output string, it does not claim a guarantee the system fails to provide. The finding itself sets needs code fix: false.

WHAT SURVIVES: genuine, verifiable documentation staleness worth a two-line fix in docs/release-checklist.md the next time that file is opened. Note the file already anticipated the restyle at :122-125 ("only after the attach-narration restyle has merged and the full checklist has been rerun against the shipped output"), which makes the omission a clear oversight rather than a disputed judgement. But it belongs in a docs-hygiene list, not in a security report at low severity with a stated consequence I can demonstrate is false. Refuted as a security finding; downgraded to informational.

(No tmux server was created during this verification.)
- **Residual 1's version pin is stale relative to the project's own evidence file** (spec-conformance; raised low, verdict not-a-finding)
  - REFUTED. The finding's mechanical facts check out, but they do not add up to a defect.

Verified mechanics (all confirmed by reading, not by trusting the quote):
- SECURITY.md:29 reads exactly as quoted: "...Enter selects the **highlighted** entry — verified 2026-08-01 (Claude Code 2.1.220, codex 0.146.0) that with the *full* payload typed, the exact match is highlighted."
- docs/release-verification-notes.md:33-48 does record a 2026-08-05 verify-live run at agentctl bb68e3b, Claude Code 2.1.222, codex-cli 0.146.0, with Claude clear / Codex clear / Compact all "recorded y".
- hack/release-verify.sh:553, 576, 599 are the operator prompts, and their text is outcome-level: "was junk visibly cleared, /clear executed, and the conversation reset?" — no popup/highlight question anywhere in verify-live.
- The popup-specific criterion lives only in verify-injection.sh's `verify` mode (hack/verify-injection.sh:267: "Did junk appear, C-u clear it, exact /clear match, and the conversation reset?"). release-verify.sh invokes verify-injection.sh at exactly one site, line 441, in `measure` mode, gated on MEASURE (set at line 362). docs/release-checklist.md:130 confirms `--measure` is "only when internal/tmuxx.payloadDelay changes" and that "The forensic verify-injection.sh rig is measure-only."

Why it still fails as a finding:

1. No gap between promise and behaviour — the direction is the opposite. SECURITY.md:29 does not assert the popup property holds on current harnesses. It asserts a dated, version-pinned historical observation, which is true and remains true. That is precisely the discipline spec §1.1 demands: report what was observed, when, and on what. A claim that carries its own scope cannot be "stale"; it can only be superseded, and nothing supersedes it. The scope rule says an overstating document is a defect. This document understates.

2. The "fresher evidence" is evidence for a different proposition. The 2026-08-05 run attests that /clear executed and the conversation reset. Residual 1's coupled-risk argument turns on which palette entry is *highlighted at the instant Enter lands* — an observation the verify-live path never captures (release-verify.sh:553/576/599 vs verify-injection.sh:267). Folding the 2026-08-05 outcome attestation into SECURITY.md:29 as support for the highlight property would be a weaker evidentiary claim dressed as a stronger one — i.e. the edit the finding recommends would move the document toward overstatement, not away from it. The finding's own text concedes the two are "stronger in one sense... and weaker in another".

3. Nothing is reachable, reproducible, or fixable. The finding states needs code fix: false, and its own "failure scenario" section contains no failure — it argues that a hypothetical reader might draw an inference the document does not invite. That is an editorial cross-reference preference, not an accident, wrong-target delivery, crash, or promise/behaviour gap.

4. Severity was inflated even on the finding's own terms. 2.1.220 → 2.1.222 is a patch bump on one of the two named harnesses; codex is unchanged at 0.146.0. There is no evidence offered, and none exists in the repo, that the popup ranking changed.

Adjacent observation, offered only so the report author can judge it separately — it is NOT this finding and I make no claim it is reportable: SECURITY.md:58 states flatly "The manual harness re-verification checklist runs before each release," while docs/release-checklist.md:3-5 makes the runbook conditional ("Run this when a release changes tmux targeting, harness startup, or injected command delivery. Otherwise tick 'Checklist not required'"). That is an unconditional-vs-conditional mismatch about process, and unlike the version pin it is a case of the security document claiming more than the process delivers. Whoever assembles the report may want to evaluate that on its own merits; it is not rescued by, and does not rescue, the finding under review.
- **"writes nothing inside application repositories" reads as broader than it is: agentctl's own launch causes repo-local AMQ state** (spec-conformance; raised informational, verdict not-a-finding)
  - Citations check out but the defect does not exist.

VERIFIED FACTS: SECURITY.md:50 is exactly as quoted. internal/tmuxx/tmux.go:130 (`new-session ... -c dir`) and :168 (`new-window ... -c dir`) are accurate; internal/fleet/fleet.go:332-346 (`resolveDirectory`) resolves dir to the operator-named path or `l.getwd()`. internal/harness/harness.go:70 builds `{"amq","coop","exec","--session",session,"--me",role,spec.Executable}`. docs/release-checklist.md:10 states AMQ auto-discovers the repo-local `.agent-mail`. /Users/rob/git/agentctl/.agent-mail exists as drwx------ and .gitignore:2 ignores it. The doc's first clause is independently true: grep for os.Create/os.WriteFile/os.MkdirAll/os.OpenFile across non-test Go files returns zero hits (only prose inside docs/superpowers/plans/2026-08-02-brew-packaging.md).

WHY REFUTED: The finding needs SECURITY.md:50 to promise more than the system delivers. It does not. The grammatical subject of "writes nothing inside application repositories" is agentctl, and the immediately following sentence in the same paragraph explicitly names "session data created by `amq coop exec`" and defers its permissions to AMQ's documented 0700/0600. The paragraph therefore already discloses the exact mechanism the finding says it hides; it is silent only on that data's location, and silence about location is not an affirmative guarantee of absence. The finding concedes in its own text that "the distinction the sentence intends (agentctl's own process writes nothing) is correct," which reduces the claim to a hypothetical misreading that the adjacent clause pre-empts.

The failure scenario carries no security consequence either. The only property SECURITY.md asserts about that data is its permissions, and that holds empirically (drwx------ measured on /Users/rob/git/agentctl/.agent-mail). A reader failing to add a .gitignore entry or to clean queue state falsifies no stated invariant. Repo-local `.agent-mail` placement is AMQ's documented behaviour, not an agentctl write: agentctl only sets a tmux pane cwd via -c, and the runbook (docs/release-checklist.md:9-11) already documents the discovery behaviour for operators. No gap between doc and code, no wrong-target delivery, no crash, no accident class. At most a copy-editing preference about sentence pairing, which does not meet the bar for a reportable finding.
- **The "exactly one shellq call site" invariant is unpinned: nothing fails if a second shell-string assembly point is added** (test-coverage; raised informational, verdict not-a-finding)
  - The finding's factual premises are accurate, but they do not add up to a defect in the code as written. Four independent reasons kill it.

1. THE FINDING REFUTES ITSELF ON THE DOC/CODE GAP. The scope rule admits "any gap between what SECURITY.md promises and what the code actually does" and treats an overstating document as a defect. There is no gap here, and the finding concedes it: "The property holds today." I re-derived it rather than trusting the quote — `grep -rn shellq --include='*.go'` over the worktree returns exactly four lines: /Users/rob/git/agentctl/internal/shellq/shellq.go:1 and :5 (the package itself), /Users/rob/git/agentctl/internal/shellq/shellq_test.go:1, the import at /Users/rob/git/agentctl/internal/fleet/fleet.go:18, and the single call at /Users/rob/git/agentctl/internal/fleet/fleet.go:264 (`return "exec " + shellq.Join(argv), nil`, inside `agentCommand`). SECURITY.md:9's claim is true of the artifact. A document that accurately describes the code is not a defect.

2. THE FAILURE SCENARIO IS NOT REPRODUCIBLE FROM THE CODE AS WRITTEN. It requires source that does not exist ("a future change adds a second shellq.Join call ... a new agentctl exec-style command"). The instruction that every finding be reproducible from the code as written excludes this by construction. The observation is about the absence of a regression guard against hypothetical future code, which is a process/hardening preference, not a property of the audited artifact.

3. THE PREMISE THAT A SECOND CALL SITE WOULD BE DANGEROUS IS WRONG. The scenario's harm hinges on tokens "sourced from a value that has NOT passed config validation" reaching `shellq.Join`. But shellq is explicitly total and does not rely on caller validation. /Users/rob/git/agentctl/internal/shellq/shellq.go:2-3 documents it: "Quote is total: it accepts arbitrary bytes and imposes no precondition on callers", with NUL the one stated exception. The implementation at shellq.go:10-12 is single-quote escaping (`"'" + strings.ReplaceAll(s, "'", `'"'"'`) + "'"`), which has no escape hatch for any byte other than NUL. And this is enforced, not merely asserted: /Users/rob/git/agentctl/internal/shellq/shellq_test.go:118-145 (`assertQuoteRoundTrip`) runs the quoted token through a real `sh -c` — `set -- <quoted>; printf '%s:' "$#"; printf %s "$1"` — and requires exactly one argument byte-identical to the input, driven by `FuzzQuoteRoundTrip` (shellq_test.go:95) over arbitrary strings with seeds including `$HOME`, backtick-whoami, `;|&<>()` and embedded newlines. So an unvalidated launch directory or a read-back `@agentctl_*` option passed through `Join` would be correctly quoted into one inert shell word. Config validation is defence in depth for other reasons (codex `--config 'model_reasoning_effort="…"'` TOML parsing, tmux name rules), not the thing that makes shellq safe.

4. THE PROPOSED GUARD DOES NOT COVER ITS OWN WORST EXAMPLE. The scenario's most plausible case is "a wrapper that pre-pends `cd <dir> && ` to the window command". That is a hand-built shell string that would not call `shellq.Join` at all, so a call-site-count assertion would pass while the injection landed. A control that misses the exemplar it was designed for is not a control.

Secondary check — is the constraint really undefended? Mostly, and I confirmed the finding's CI claim: /Users/rob/git/agentctl/.github/workflows/ci.yml runs `go test`, `go vet`, shellcheck, golangci-lint v2.12.2, integration tests and goreleaser, with no import-graph or call-site rule; /Users/rob/git/agentctl/.golangci.yml contains only an errcheck `exclude-functions` block for `fmt.Fprint*` — no `depguard`, no `forbidigo`, no custom linter. But "undefended by automation" is not the same as "undefended": /Users/rob/git/agentctl/CLAUDE.md Hard constraints and Security-relevant changes both name the single-site rule and require reading the whole path before editing it, and the design spec treats a "second shell-composition site (§12.1)" as an explicit rejection criterion in a live decision record (docs/superpowers/specs/2026-08-01-agentctl-design.md:478, where relocating the iTerm2 menu was rejected partly on that ground). The invariant is a reviewed contract in the mandatory instruction file, not merely "the reviewer's only defense is remembering the constraint."

Net: no accident, no wrong-target delivery, no crash, no doc overstatement. At most this is a worthwhile hardening idea — a `go vet`-adjacent or grep-based CI assertion that `shellq` has one importer — but "needs code fix: true" is not defensible and this must not reach the report as a finding.
- **The "send-keys appears only inside DeliverPayload" invariant is unpinned by the same structural gap** (test-coverage; raised informational, verdict not-a-finding)
  - Every factual claim in the finding checks out, and none of them add up to a defect.

Verified: internal/tmuxx/control.go:23, :26 and :38 are the only three non-test `send-keys` sites, all inside Client.DeliverPayload; a repo-wide grep for "send-keys" over *.go/*.md/*.sh finds nothing else outside tests, hack/ probes and docs. internal/control/dispatcher.go:33-41 feeds DeliverPayload only `command.Payload` from `Lookup(operation)`, and internal/control/registry.go:22-25 is a closed two-entry table of untyped constants ("/clear", "/compact"). It is also true that no test enforces the absence: there is no AST/source-walking test (grep for go/ast, go/parser, packages.Load, filepath.Walk over *.go returns nothing) and .golangci.yml (version 2) configures only errcheck exclusions, no forbidigo rule.

But the finding's own "what the code does" paragraph is a description of the code being CORRECT. SECURITY.md:11 promises that no option, environment variable, or stdin path lets caller-supplied text reach `tmux send-keys`, and that is true of every path in the tree today. There is no gap between the document and the behaviour, so the scope rule's doc-overstatement category does not apply, and the finding is not an accident, a wrong-target delivery, or a crash either.

The failure scenario is not reachable from the code as written. It requires someone to write a new subcommand in a future commit. That is a hypothesis about a commit that does not exist, not a property of this artifact. It is also harder to do accidentally than the finding implies: Client.runner is unexported (internal/tmuxx/runner.go), so a bypass cannot be a stray line in cmd/agentctl — it would have to be new exported surface added inside internal/tmuxx, which is precisely what AGENTS.md:40 forbids by name ("Never add a path for caller-supplied text, arbitrary slash commands, or raw keys to reach tmux send-keys").

The project has already considered and settled the enforcement question the opposite way. docs/superpowers/specs/2026-08-01-agentctl-design.md:1082-1084 states the containment argument explicitly: "the narrower the exposed surface, the fewer places a reviewer must check that caller-supplied text cannot reach send-keys. A general-purpose exported SendKeys would reopen precisely the hole the registry closes." The chosen mechanism is the narrow single-operation API plus reviewer attention, not a source-grep test. Asking for the test is a process preference, not a demonstrated weakness, and "needs code fix: true" is indefensible when no current line is wrong.

Finally, the shape of the argument generalises without limit — "no test asserts that no future code invokes a shell", "no test asserts no future registry entry carries a format verb", "no test asserts shellq.Join gains no second call site" — which is the tell that it is a wish about future-proofing rather than a finding about this codebase. It should not reach the report.
- **Zero concurrency coverage and no `-race` in CI** (test-coverage; raised informational, verdict not-a-finding)
  - Human meatbag: this one does not survive contact with the code. Three of its supporting claims are false, and what remains is not a finding under the scope rule.

WHAT IS TRUE (verified):
- No concurrency in production code. `grep -rnE '^\s*go [a-zA-Z_(]' --include='*.go'` over the whole repo returns ZERO hits — not just in production, but in tests too. There is no goroutine anywhere in agentctl.
- The only `sync.` usages outside the testing package are internal/tmuxx/runner.go:70 (FakeRunner.mu) and cmd/agentctl/integration_fixture_test.go:87 (socketRunner.failureMu). Both are in test doubles.
- CI runs `go test ./...` at .github/workflows/ci.yml:23 and `go test -tags integration ./...` at :75, neither with `-race`. Confirmed by reading the file.
- 185 `t.Parallel()` calls exist across the suite.

WHAT IS FALSE:
1. "`grep -rn '\-race'` over the repo returns nothing." It does not. Race-enabled runs are an explicit, checked-off development gate in the project's own plans: docs/superpowers/plans/2026-08-01-launch-no-server.md:50 ("- [x] Run focused race-enabled tests"), docs/superpowers/plans/2026-08-01-target-validation.md:159-161 (`go test -race ./internal/config ./internal/target`), docs/superpowers/plans/2026-08-01-integration-suite.md:185, docs/superpowers/plans/2026-08-01-version-command.md:243 (`go test -race ./... -count=1`), docs/superpowers/plans/2026-08-01-release-verification.md:446. The premise that races have received zero attention is wrong.
2. "the integration socketRunner's failureMu at cmd/agentctl/integration_fixture_test.go:87 [is exercised] concurrently." `grep -c 't.Parallel' cmd/agentctl/integration_*_test.go` returns 0 for all four integration files. The integration suite is entirely serial, and each fixture constructs its own socketRunner at integration_fixture_test.go:324 anyway. This claim is flatly wrong.
3. "the suite already exercises shared state (FakeRunner instances) ... concurrently." It does not. Every FakeRunner is constructed inside the parallel subtest closure, after `t.Parallel()` — e.g. cmd/agentctl/main_attach_test.go:28-30. Each parallel test owns a private instance; nothing is shared across them. FakeRunner.mu is never contended.

EMPIRICAL CHECK: I ran `go test -race ./...` on this worktree. All 16 packages pass clean (cmd/agentctl 6.3s, internal/tmuxx 10.0s, hack 29.8s, etc.). There is no latent race for the detector to find.

WHY IT IS NOT A FINDING: The finding concedes in its own "what the doc says" field that SECURITY.md makes no concurrency claim and that this is "correct[]". So there is no gap between promise and implementation — which is one of the four things the scope rule says counts. There is no accident, no wrong-target delivery, and no crash: there is no concurrent code path at all. The failure scenario is explicitly conditional on work that does not exist ("A future change introduces a goroutine — the most likely candidate is parallelising `status --all`"), so it is not reproducible from the code as written, and `needs code fix: true` asserts a fix for a defect that is absent.

RESIDUE: The single defensible observation is that CI does not pass `-race`, while the project's plan documents treat race-enabled runs as a gate. That is a CI-hygiene nit for the maintainer, not a security finding, and it costs nothing to add given the suite runs clean today. It does not belong in a security audit report.
- **SECURITY.md calls the quoting layer "fuzz-tested", but the fuzz target only ever runs its 12 checked-in seeds — no corpus, no `-fuzz` anywhere** (test-coverage; raised informational, verdict not-a-finding)
  - REFUTED. The mechanical observations are all true, but they do not add up to an overstatement, and the finding's one concrete failure scenario is contradicted by a test it did not read.

What I confirmed (so the auditor's facts are not in dispute):
- `FuzzQuoteRoundTrip` exists at /Users/rob/git/agentctl/.worktrees/issue-81-security-review/internal/shellq/shellq_test.go:95 with 12 `f.Add` seeds (:96-109) and a real-`sh` differential oracle at :124-125 (`set -- <quoted>; printf '%s:' "$#"; printf %s "$1"`).
- `grep -rni fuzz` over the repo returns only SECURITY.md:9, shellq_test.go:95/:113, docs/security/2026-08-05-merged-v1-security-review.md:36, and design spec :216/:853/:886. `.github/workflows/ci.yml` invokes `go test ./...` (line 23) and `go test -tags integration ./...` (line 75); no `-fuzz` anywhere.
- No `testdata/fuzz/` directory exists (`find` returns only ./hack/testdata).
- Confirmed by running it: `go test ./internal/shellq/ -run FuzzQuoteRoundTrip -v` executes exactly seed#0..seed#11 as ordinary subtests.

Why it is not a finding:

1. The finding's own concrete regression example is caught by an existing test. It argues "a fast path `if isAlnum(s) { return s }` passes every seed and every table case while silently emitting an unquoted word." That is false. shellq_test.go:19 asserts `Quote("agent123")` == `'agent123'` — an *exact-output* assertion on an all-alphanumeric input. The proposed fast path fails that subtest immediately. The finding's coverage tally ("12 seeds plus the 8 table cases at :42") omits `TestQuote` at :10-40 entirely — 12 exact-output cases covering empty, lone quote, embedded quote, backslash, double quote, `$HOME`, backticks, spaces, newline, `;|&<>()`, and leading dash. Those are strictly stronger than a round-trip property: they pin the escaping strategy byte-for-byte, so the other named refactor (switching to double-quote escaping) fails all 12 on the first run. The regression class the finding says fuzzing is needed to catch is precisely the class the table tests already catch.

2. SECURITY.md does not promise what the finding says it promises. SECURITY.md:9 says the window command "is assembled by a dedicated, fuzz-tested quoting layer from tokens that have already passed these checks." A dedicated `FuzzXxx` target with a live-`sh` oracle that runs on every `go test ./...` *is* what "fuzz-tested" denotes in Go. The sentence claims no CI fuzzing campaign, no time budget, no corpus, and no continuous coverage. The inference the finding attributes to a reader ("continuous fuzz coverage will catch a regression") is supplied by the reader, not by the document. The design spec is even more explicit and equally accurate: §853 says "table tests + Go fuzz test asserting the exactly-one-word round-trip property," and §886 documents the NUL exclusion that shellq_test.go:120-122 implements. There is no gap between doc and code.

3. The absence of `testdata/fuzz/FuzzQuoteRoundTrip` is not evidence of a gap. Go writes that directory only when fuzzing finds a failing input; the generated corpus otherwise lives in GOCACHE, outside the repo by design. An empty corpus directory is the expected state for a target with no discovered crashers, so its absence supports nothing either way.

4. The artifact under review is a 1-line total function. shellq.go:11 is `return "'" + strings.ReplaceAll(s, "'", `'"'"'`) + "'"`, documented total at shellq.go:2-4. Its correctness is establishable by inspection over the whole input domain (single-quoted POSIX words have exactly one escape case), which is why the residual value of a long fuzzing campaign here is close to zero. Fuzzing pays off on parsers with state; this has none.

5. No failure scenario, present or reachable. The finding itself concedes "not an exploit." Its scenario is conditional on a hypothetical future edit that a currently-passing test already blocks. Under the issue's scope rule, a doc counts as a defect when it *overstates its guarantees*; this one states, accurately, that a fuzz test exists. Reporting it would put a testing-practice preference (run `-fuzz` in CI, commit a corpus) into a security report as a promise-vs-reality gap, which it is not.

If anything survives, it is a non-security suggestion — adding a nightly `go test -fuzz=FuzzQuoteRoundTrip -fuzztime=1m` job would be a reasonable hardening nicety — but that is an enhancement request, not a defect, and it does not belong in a security review's finding list.
- **ValidateRoleName's own rejection table omits the trailing-newline case that the session and model tables both carry** (test-coverage; raised informational, verdict not-a-finding)
  - REFUTED — this is a test-table completeness suggestion, not a defect.

1. The claimed code behaviour is correct and the finder admits it. internal/config/config.go:16 compiles ONE package-level `nameExpression` from `namePattern` (config.go:11). ValidateSessionName (config.go:103) and ValidateRoleName (config.go:116) both call `nameExpression.MatchString` on that same object, as do parseRoles (config.go:223), applyModels (config.go:272) and applyEfforts (config.go:316). Go's RE2 `$` is end-of-text without the `m` flag, so "planner\n" is rejected for roles today.

2. There is no gap between the document and the code. SECURITY.md:9 promises role names are validated against `^[a-z0-9][a-z0-9_-]*$`; internal/config/config.go:116 does exactly that. The scope rule admits only accidents, wrong-target delivery, crashes/panics, and doc-vs-code gaps. This is none of the four. The finder's own text says "Not currently exploitable — the shared regex makes the case safe today."

3. The existing suite already pins the behaviour the finding worries about. internal/config/config_test.go:35 (`{name: "trailing newline", value: "planner\n"}`) exercises the identical regexp object that ValidateRoleName uses; I ran `go test ./internal/config/ -run 'TestValidateRoleName|TestValidateSessionName' -v` and TestValidateSessionNameRejectsInvalidNames/trailing_newline PASSES. A row in the role table would assert the same property of the same variable. The role table at config_test.go:81 does carry the embedded-newline case ("plan\nner"), and config_test.go:142 carries it again for the --roles list path.

4. The failure scenario is not reachable from the code as written. It is conditional on a hypothetical future refactor ("gives roles their own pattern", "a hand-written character loop", "a multiline-flagged pattern"). Speculation about code that does not exist is not a reproducible finding, and any such refactor would itself be a security-relevant change that CLAUDE.md already routes through the SECURITY.md review path.

5. Even the hypothetical is defended downstream. Per the measurements established for this audit, tmux 3.7b REFUSES window names containing a newline, so a trailing-newline role could not become the `-n role` window name at internal/tmuxx/tmux.go:130 (verified: `arguments := []string{"new-session", "-d", "-s", name, "-n", role, "-c", dir}`). The target chain at internal/target/resolver.go:41 calls ValidateRoleName before any tmux interaction, and the exact-match comparison at resolver.go:65 (`if window.Name == role`) plus the metadata gate at resolver.go:74 (`window.Managed != "1" || window.Role != role`) fail closed on mismatch.

6. "needs code fix: true" is wrong on its own terms — the finder proposes no code change, only an extra test row. Nothing in internal/config is defective.

The most that survives is a low-value suggestion to add `"planner\n"` to config_test.go:81 for table symmetry. That does not clear the bar for the report.
